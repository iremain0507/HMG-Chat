P0-T1-01 | AWS 실계정 프로비저닝 필요(VPC/RDS/Redis/ECR/ECS) — project.config.yaml 에 AWS=LOCAL_ONLY 로 배포 직전까지 보류 결정 명시(aws.account_*="LOCAL_ONLY_*_PENDING"), 자격증명도 없어 자율 실행 불가. Human gate 필요.
P5-T4-02 | converter-worker(Python/poetry) 단위테스트 RED/GREEN 실행 불가 — 이 세션은 python3 실행을 --version 조회 이외(-c/-m/스크립트 위치인자 모두) 승인 필요로 차단하고, poetry/pytest 도 미설치(pip/poetry install 도 승인 필요라 설치 불가, 대화형 human 부재라 승인 영구 대기). scripts/verify-gates.sh 는 pnpm 기반이라 apps/converter-worker(pnpm workspace 밖, poetry.lock)를 아예 검증 못 함 — pytest 실행 자체가 유일한 검증 경로인데 세션 권한상 원천 차단. 테스트 없이 skeleton 만 만들면 RED 증거 규칙 위반이라 미구현 상태로 격리. Human gate 필요(세션에 python 실행 승인 또는 poetry/pytest 사전설치).
P5-T6-01 | web PptxRenderer 가 acceptance("10MB PPTX artifact 가 web preview 에서 5초 이내 표시")를 만족하려면 실제 pptx→pdf 변환 결과물을 서버에서 받아와야 하는데, 그 경로(apps/server/src/lib/office-pdf-converter.ts, converter-worker HTTP client, L17)가 아직 없고 이를 만드는 것도 결국 converter-worker 자체(P5-T4-02, 방금 격리)에 의존 — 동일 근본원인 다운스트림 차단(P4-T3-02→P4-T6-01 과 동일 패턴). PdfRenderer/ArtifactPanel 만 부분구현하면 태스크의 실제 acceptance(pptx 미리보기)를 우회/약화하는 셈이라 미구현 상태로 격리. P5-T4-02 unblock 후 재시도.
