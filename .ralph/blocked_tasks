P0-T1-01 | AWS 실계정 프로비저닝 필요(VPC/RDS/Redis/ECR/ECS) — project.config.yaml 에 AWS=LOCAL_ONLY 로 배포 직전까지 보류 결정 명시(aws.account_*="LOCAL_ONLY_*_PENDING"), 자격증명도 없어 자율 실행 불가. Human gate 필요.
P9-T6-02 | e2e Playwright + A11y audit — 전체 스택(server+web+DB) 기동 + 브라우저 e2e 필요. 코드는 준비 가능하나 LOCAL_ONLY 세션에서 실 e2e green 확정은 배포/스테이징 환경에서. 배포-시 human gate.
P9-ALL-01 | GA 게이트: 커버리지 임계 + docs/decisions ADR INDEX 는 가능하나 "24시간 staging soak 오류 0" 요건이 세션에서 불가(장시간 실행+스테이징 환경). 배포-시 human gate.
P9-ALL-02 | v1.0 GA production 배포 = AWS(LOCAL_ONLY 라 불가) + 회고 ADR. 실 배포는 사람이 AWS 프로비저닝(P0-T1-01) 후 수행. 배포-시 human gate.
P14-T2-01(temperature) | temperature 런타임 반영은 RunTurnInput/ChatInput 에 temperature 필드를 추가해 orchestrator.ts(provider.chat 호출부)에서 forward 해야 하나, 이 phase-loop-prompt 의 P14-T2-01 표는 편집 허용 파일을 routes/messages.ts, app.ts 로만 한정(orchestrator.ts 는 표 밖). maxTokens/systemPrompt/defaultModel(테스트된 acceptance)은 완료 — temperature 배선만 별도 후속 태스크로 격리.
P14-T2-02(내장도구 org-scoped 동적 toolMaxTokens) | assembleBuiltinTools(app.ts) 는 app 조립 시점(요청/org 이전) 싱글톤이라 org_settings.toolMaxTokens 를 요청별로 반영 불가 — 반영하려면 messages.ts 의 정적 tools 배열을 요청마다 재조립하거나 tool handler(deep-research-handler.ts 등) invoke-time 조회(T3-01 패턴)로 옮겨야 하나 둘 다 이 태스크 표(app.ts, memory-extractor.ts) 밖 파일. app.ts 는 DEFAULT_ORG_SETTINGS.toolMaxTokens 단일출처 참조로 정리(매직넘버 제거)만 완료, memory-extractor.ts 는 실제 파라미터화(기본 4096, 커스텀 값 반영) 완료. 실 org-scoped 동적 배선은 후속 태스크로 격리.
P14-T3-02(index 시 resolved settings 를 chunkText 에 실제 전달) | chunker.ts 는 ChunkOptions(chunkSizeTokens/overlapTokens) 를 이미 지원하고 기본값을 DEFAULT_ORG_SETTINGS(단일출처)로 정합화했으나, 실제 index 호출부 db/document-service.ts:159 `chunkText(parsed.markdown)` 는 이 태스크 표(knowledge/chunker.ts 만) 밖 파일이라 org settingsService.resolve(orgId) 결과를 여기 전달하는 배선은 범위 밖. 후속 태스크로 격리(P14-T2-02 toolMaxTokens 격리와 동일 사유 패턴).
P15-T3-01(index 시 chunker 설정 반영) | chunkText 호출부(chunkText(parsed.markdown), 실제 org-scoped ChunkOptions 를 넣어야 할 지점)는 apps/server/src/db/document-service.ts:159 이며, 이 파일을 org settingsService.resolve(orgId) 로 배선하려면 (1) document-service.ts 의 indexDocument/DocumentIndexingDeps 를 확장(settings resolver 주입 + chunkText 호출에 opts 전달)해야 하고 (2) app.ts 의 createDocumentRoutes({...}) 호출에 settings: settingsService 를 주입해야 함. 두 파일 모두 이 태스크 표(apps/server/src/knowledge/**, 필요 시 routes/documents.ts)밖 — routes/documents.ts 단독 수정만으로는 document-service.ts 내부의 정적 chunkText(parsed.markdown) 호출에 영향을 줄 방법이 없음(인덱싱 트랜잭션·에러 핸들링이 전부 document-service.ts 안에 캡슐화). P14-T3-02 가 동일 파일/라인에 대해 동일 사유로 이미 격리된 것과 같은 구조적 제약(표 확장 또는 human gate 필요). 후속 태스크로 격리.
P17-T6-07(revoked 공유 페이지, TS-21) | 만료(410 EXPIRED)와 취소(410 REVOKED)를 프론트에서 다른 화면(만료=시계 아이콘, 취소=❌)으로 구분하려면, 서버가 GET /api/v1/share/:token 410 응답에 원인(reason)을 실어야 한다 — 현재 apps/server/src/db/artifact-share-service.ts:81 resolvePublicShare()는 found.revokedAt/found.expiresAt<=now 를 이미 내부적으로 판별하지만 둘 다 동일한 ArtifactShareServiceError("GONE", ...)로 뭉뚱그려 던지고(routes/public-share.ts:60-68 errorJson 도 category만 전달), reason 필드가 wire format에 없다. 이 서버측 변경(db/artifact-share-service.ts, routes/public-share.ts)은 이 태스크 표(apps/web/src/app/share/[token]/**, components/share/**)밖이자 T1 소유 경로라 T6 태스크에서 수정 불가(path ownership). 서버가 reason 을 내려주기 전까지는 프론트가 만료/취소를 구분할 데이터 자체가 없어 TS-21 #3/#4 acceptance(클럭 vs ❌ 페이지 분기)를 완성할 수 없음. 후속 T1 태스크로 격리(예: P17-T1-07 — GONE 에러에 reason:"expired"|"revoked" 추가 + wire format 노출) 후 이 T6 태스크 재개 필요.
P22-T1-14 | WON'T-BUILD (계약배치 승인됨) — 인앱 Python 플러그인 런타임은 임의코드 실행 표면. 확장성은 MCP+Skills+내장툴로 고정(20-MULTI-AGENT-TOOL). 영구 격리.
P22-T6-11 | WON'T-BUILD (계약배치 승인됨) — Arena A/B + ELO 리더보드. 단일조직 실효 낮음, 피드백 👍/👎 이미 존재. 영구 격리.
P22-T6-20 | WON'T-BUILD (계약배치 승인됨) — 핸즈프리 음성/영상 통화(WebRTC 신규 전송계층). STT/TTS 로 기본 음성 충족. 영구 격리.
P22-T1-12 | 서버측 3/4 acceptance 완료·커밋(등록 REST + spec→AgentToolSpec 변환 + invoke-time SSRF 재검증 + 채팅턴 org-scoped 조립, migration 0032, 152 tests green). 남은 것은 4번째 acceptance 인 ★BROWSER(admin 패널에서 spec 등록 → 채팅에서 endpoint 호출 라운드트립) 뿐인데, 그 UI 는 apps/web/src/components/admin/** (T6 소유 경로)라 team=T1 인 이 태스크에서 수정 불가(path ownership). 카탈로그 files 힌트도 apps/server/src/tools/ 로 한정. P17-T6-07 과 동일한 서버/프론트 분할 구조 — 후속 T6 태스크(예: P22-T6-21 OpenAPI 툴서버 admin 패널: mcp-servers 패널 미러 + /preview Playwright 스펙) 신설 후 (A)+(B) 충족 시 passes=true 가능. 서버 API 는 이미 준비 완료(GET/POST /api/v1/openapi-tool-servers 마운트됨).
P22-T1-11 | 서버측 3/4 acceptance 완료·커밋(계약배치 C14 승인 범위). 구현: lib/ldap-client.ts(LdapDirectoryClient 인터페이스 + RFC4515 필터 이스케이프 + 그룹→롤 매핑 + in-memory dev-stub + ldapts 실 transport), routes/auth.ts POST /login/directory(bind→유저 자동 프로비저닝/롤 동기화, 401 자격증명 vs 503 인프라 구분, 비밀번호 로그인과 공유하는 brute-force 429, 디렉터리 반환 이메일 도메인으로 org 재판정 = body 로 cross-org 불가), routes/admin-settings.ts POST /ldap/test, org-settings-schema.ts ldap* 필드(기본 ldapEnabled=false 라 기존 인증 경로 비파괴), app.ts 양쪽 주입 + routes-mounted EXPECTED_ROUTES 2건 추가. 59 tests green, 4-gate exit 0. 남은 것은 4번째 acceptance 인 ★BROWSER(admin Settings/Identity 패널에서 host/baseDN/bindDN/그룹필터 입력 → Test/Save → 설정 persist + 연결테스트 성공)뿐인데, 그 UI 는 apps/web/src/components/admin/** (T6 소유 경로)라 team=T1 인 이 태스크에서 수정 불가(path ownership). 카탈로그 files 힌트도 apps/server/src/routes/auth.ts 로 한정. P22-T1-12·P17-T6-07 과 동일한 서버/프론트 분할 구조 — 후속 T6 태스크(예: P22-T6-22 LDAP/Identity admin 패널: admin-settings 패널 미러 + /ldap/test 연결테스트 버튼 + /preview Playwright 스펙) 신설 후 (A)+(B) 충족 시 passes=true 가능. 서버 API 준비 완료. 추가 주의: 승인 dep `ldapts` 는 LOCAL_ONLY 라 미설치 — 실 transport 는 동적 import 로 지연 로드되고 미설치 시 503 로 표면화(embedding dev-stub/object-store 와 동일 패턴). 배포 시 `pnpm add ldapts` 필요.
