P0-T1-01 | AWS 실계정 프로비저닝 필요(VPC/RDS/Redis/ECR/ECS) — project.config.yaml 에 AWS=LOCAL_ONLY 로 배포 직전까지 보류 결정 명시(aws.account_*="LOCAL_ONLY_*_PENDING"), 자격증명도 없어 자율 실행 불가. Human gate 필요.
P4-T3-02 | knowledge/parser-pipeline+pdf/pptx/docx/xlsx-parser — 14-INTERFACES.md 에 ParserPipeline/ParsedDocument 타입 미정의(single source of truth 없음). 03-ARCHITECTURE.md 는 PPTX/DOCX=Python(python-pptx/python-docx, converter-worker 경유 LibreOffice)+PDF=외부 Gemini VLM API(v1.0 default, LOCAL_ONLY dev-stub 미정의)+XLSX=openpyxl(Python AST) 로 명시해 TS apps/server/src/knowledge/*.ts(T3 소유 경로) 구현과 아키텍처가 불일치, npm 의존성도 미지정. 08-SPRINT-PLAN.md 자체가 이 phase 를 "가장 시행착오 많음(3 MR 폐기)" 로 표시하며 프로덕션 전 spike/rag-architecture 브랜치 3일 prototype 을 권장 — human 아키텍처 결정(파서 전략: TS 네이티브 라이브러리 채택 vs converter-worker 위임 vs VLM dev-stub 설계) 필요.
P4-T6-01 | web/projects/[id]/documents upload UI + indexing progress — acceptance("업로드 → indexed, 30페이지 PDF < 60초")가 실제 인덱싱 완료를 요구하나 이는 `POST /projects/:id/documents`(multipart→parser-pipeline 큐잉, 16-API-CONTRACT.md § 5) 백엔드에 의존. 그 라우트는 P4-T3-02(parser-pipeline, 위 항목)가 구현해야 하며 routes/documents.ts 는 목록/조회/삭제만 존재(POST 는 P4-T3-07 desc 에서 명시적으로 범위 밖, "생성은 parser-pipeline 의존" 주석 참조). T6=apps/web/src/ path ownership 상 백엔드 라우트 작성 불가하고, 07-AGENT-TEAMS.md § 293 의 "mock client 로 일찍 시작 가능" 도 이 태스크엔 부적용 — acceptance 가 dev 서버에서의 실제 indexed 완료를 요구해 mock 으로 충족 불가. P4-T3-02 와 동일 근본원인(파서 아키텍처 human 결정 필요)의 다운스트림 차단.
