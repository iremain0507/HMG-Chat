P0-T1-01 | AWS 실계정 프로비저닝 필요(VPC/RDS/Redis/ECR/ECS) — project.config.yaml 에 AWS=LOCAL_ONLY 로 배포 직전까지 보류 결정 명시(aws.account_*="LOCAL_ONLY_*_PENDING"), 자격증명도 없어 자율 실행 불가. Human gate 필요.
P9-T6-01 | 16-API-CONTRACT.md § 14 GET /admin/dashboard, GET /admin/users, PATCH /admin/users/:id, POST /admin/users/:id/suspend|unsuspend, GET /admin/tool-metrics 가 apps/server/src/routes/admin.ts 에 미구현(health/history 만 존재, P9-T1-02 desc 범위 밖) — 이 5개 엔드포인트를 다루는 T1 feature_list 태스크가 존재하지 않아 백엔드가 만들어질 경로가 없음. T6=apps/web/src/ path ownership 상 라우트 직접 구현 불가(P4-T3-02→P4-T6-01 과 동일한 다운스트림 차단 패턴). Human gate 필요(신규 T1 태스크 추가 또는 스코프 조정 결정).
