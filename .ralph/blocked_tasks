P0-T1-01 | AWS 실계정 프로비저닝 필요(VPC/RDS/Redis/ECR/ECS) — project.config.yaml 에 AWS=LOCAL_ONLY 로 배포 직전까지 보류 결정 명시(aws.account_*="LOCAL_ONLY_*_PENDING"), 자격증명도 없어 자율 실행 불가. Human gate 필요.
P1-T1-01 | RLS 통합 테스트(src/__tests__/integration/rls.test.ts) 실행에 실 Postgres 필요 — docker-compose.local.yml 로 provisioning 해야 하나 이 세션엔 docker CLI 자체가 없고(`docker` command not found) 로컬 5432 포트에 대체 Postgres 도 없음(ECONNREFUSED 확인). 마이그레이션 0001_identity.sql + RLS policy 는 06-DATA-MODEL § 부록 A 그대로 작성 완료(코드 리뷰 상 완전) — 실행 검증만 불가. Human gate 필요(Docker 설치 또는 CI service container 로 검증 위임).
P1-T1-02 | 마이그레이션 0012_password_or_magic.sql 은 06-DATA-MODEL.md § 0012 본문 그대로 작성 완료 + 정적 검증 테스트(db/__tests__/0012-password-or-magic.test.ts) 5개 RED(파일 부재 확인)→GREEN. 그러나 acceptance의 "빈 DB 에서 pnpm db:migrate 0 exit"는 실 Postgres 필요 — P1-T1-01 과 동일 인프라 제약(docker CLI 부재, 로컬 5432 대체 Postgres 없음, ECONNREFUSED). SQL 문법/FK 유효성은 실제 drizzle-kit migrate 실행 전까진 완전 검증 불가. Human gate 필요(Docker 설치 또는 CI service container 로 실행 검증 위임 — P1-T1-01 과 함께 해소 가능).
