# P22 · 미개발/미완료 기능 갭 카탈로그 (Open WebUI 파리티 + in-plan 완성)

> 이 문서는 `PROMPT.P22.md` 루프가 소비하는 **근거 대장**이다. 각 `P22-*` feature_list 항목의 file:line 근거·구현지침·acceptance·검증방식(브라우저 필요 여부)의 단일 출처.

## 생성 방법 (재현 가능)

- Workflow `wchat-gap-research` (run wf_b6118848-61c): Open WebUI 8도메인 census(웹) + rebuild_plan 의도범위 추출 + 코드베이스 감사 5종 → 후보 52 → **갭별 adversarial 코드베이스 검증**(이미 구현됐는지 반증 시도) → 검증된 47 + 미검증 4.
- 검증 통계: census 8도메인 / 후보 52 / 확정 47 / false-positive 1(`org-dynamic-toolmaxtokens` = 이미 구현, 제외) / 검증실패(스키마) 4(미검증으로 편입).

## 스코프 규칙 (사용자 확정)

- **Open WebUI 에 실제 개발된 기능이면 v2 로드맵 라벨이어도 개발 범위에 포함.** (예: 시각화/멀티모델/음성/i18n/채널/노트 등)
- **구현방식·사용자 상호작용·UI/UX 는 Open WebUI 를 레퍼런스.** 단 시각 디자인은 `apps/web/DESIGN.md` 시맨틱 토큰 + 현대위아 CI 유지(하드코딩 hex 금지, 로고 재현 금지).
- **제외(배포/human-gate·비-OWUI, 기존 blocked 유지)**: `aws-provisioning`(P0-T1-01), `ga-release-gate`(P9-ALL-01/02), `a11y-playwright-gate`(P9-T6-02). 이들은 P22 태스크가 아니라 배포시 human gate.

## 범례

- **verdict**: confirmed-undeveloped(전무) · confirmed-incomplete(스텁/반쪽배선) · partial-refine(있으나 미흡) · UNVERIFIED(워크플로우 검증 실패 — 루프 시작 시 코드베이스로 실재 재확인 후 진행/격리).
- **★needsBrowser**: 완성 검증에 실제 프론트 상호작용 또는 admin 설정이 필요 → 브라우저 UAT 필수(§ PROMPT.P22 브라우저 검증).
- **★계약의존**: `packages/interfaces`·`packages/shared`·DB migration 변경 필요 → `P22-C-01` 계약배치 승인(`.ralph/CONTRACT_APPROVED`) 전에는 격리.
- **team**: T1 platform(routes/tools/mcp/lib) · T2 orchestrator · T3 knowledge · T4 artifact · T5 skills · T6 web · RC 계약배치.

## 요약 (총 49 태스크)

- Tier1 in-plan 완성 15 · Tier2 OWUI 파리티 13 · Tier0 계약배치 1 · Tier3 계약의존 20
- 팀별: T1=17 · T2=3 · T3=5 · T4=3 · T6=20 · RC(계약)=1
- ★needsBrowser 23 · ★계약의존 20 · UNVERIFIED 4

## Tier 1 — in-plan 완성 (계약 변경 불필요, 즉시 빌드, 최우선)

| id          | team | eff | browser | contract | title                                                        |
| ----------- | ---- | --- | ------- | -------- | ------------------------------------------------------------ |
| `P22-T1-01` | T1   | M   | ·       | ·        | DELETE /auth/me self-service account deletion                |
| `P22-T1-02` | T1   | M   | ·       | ·        | Wire periodic alert rule-evaluation loop at bootstrap…       |
| `P22-T1-03` | T1   | M   | ·       | ·        | Data-retention job defined but never scheduled/invoked       |
| `P22-T1-04` | T1   | S   | ·       | ·        | POST /sessions explicit session create                       |
| `P22-T1-05` | T1   | S   | ·       | ·        | GET /sessions/:id single-session read                        |
| `P22-T1-06` | T1   | S   | ·       | ·        | Admin new-user webhook has only a record-only dev-stub       |
| `P22-T2-01` | T2   | S   | ·       | ·        | Google/Gemini provider silently drops temperature and topP   |
| `P22-T2-02` | T2   | M   | ·       | ·        | GET /notifications SSE notification stream                   |
| `P22-T2-03` | T2   | L   | ·       | ·        | Redis-backed abort/resume/HITL runtime state selectable at…  |
| `P22-T3-01` | T3   | M   | ·       | ·        | knowledge_search unified retrieval over project + ephemeral… |
| `P22-T3-02` | T3   | M   | ·       | ·        | Serve project-document CRUD off the nested contract path…    |
| `P22-T3-03` | T3   | M   | ·       | ·        | Ephemeral (attachment) indexing ignores org chunk settings   |
| `P22-T3-04` | T3   | S   | ·       | ·        | Org-scoped RAG chunk settings not applied at…                |
| `P22-T4-01` | T4   | M   | ·       | ·        | artifact_create routes large/binary content to S3…           |
| `P22-T4-02` | T4   | M   | ·       | ·        | Public share 410 cannot distinguish expired vs revoked       |

### P22-T1-01 — DELETE /auth/me self-service account deletion

verdict=**confirmed-undeveloped** · team=T1 · effort=M · kind=undeveloped · scope=in-plan

**무엇/왜**: Contract defines DELETE /auth/me (§400) but only GET(:381)/PATCH(:429) of /me exist. No delete handler and nothing mounted. Done = authenticated self-delete cascading user-owned rows under RLS, with a route + integration test.
**근거(코드베이스 검증)**: apps/server/src/routes/auth.ts has GET /me (L381) and PATCH /me (L429) but NO delete handler for /me -- the router createAuthRoutes (L99-588) contains only signup, magic-link, magic-link/verify, dev-login, me(GET,PATCH), logout, refresh. rebuild_plan/16-API-CONTRACT.md fully specifies the DELETE /auth/me section (confirmation string DELETE_MY_ACCOUNT, 202 with scheduledHardDeleteAt+ticketId, 30d grace + cascade + force-logout, 400 INVALID_CONFIRMATION). apps/server/src/**tests**/routes-mounted.test.ts EXPECTED_ROUTES lists GET(L36)+PATCH(L37) /api/v1/auth/me but no DELETE. apps/server/src/routes/**tests**/auth.test.ts has zero DELETE /me test (the delete grep hits are just in-memory Repo fakes). No deletion_request / scheduled_hard_delete migration exists. admin.ts L225 delete /users/:id is the ADMIN path (adminDa.deleteUser soft-sets status=deleted + revokes tokens, db/admin-data-access.ts:193), not self-service. Reuse ready: RefreshTokenFamilyRepo.revokeAllForUser exists (packages/interfaces/src/types.ts:623); Repo<User>.update/delete exist (types.ts:69,74); AuthDataAccess already Picks users+refreshTokenFamilies+withRlsContext, so the minimal in-plan route needs NO packages/interfaces change.
**구현 지침**: Add a DELETE /me handler in apps/server/src/routes/auth.ts (T1-owned, same file as GET/PATCH /me). Handler: call authenticate() -> 401 UNAUTHENTICATED if no session; parse body {confirmation}; if confirmation !== DELETE_MY_ACCOUNT return 400 with code INVALID_CONFIRMATION (contract-specified code, NOT generic INVALID_INPUT). On success: soft-delete self via deps.da.users.update(auth.sub, {status:deleted}) wrapped in deps.da.withRlsContext, mirroring the admin pattern in db/admin-data-access.ts:193; force-logout all sessions via deps.da.refreshTokenFamilies.revokeAllForUser(auth.sub, logout) which ALREADY exists at packages/interfaces/src/types.ts:623; delete the at/rt cookies; return 202 with { data: { scheduledHardDeleteAt: now+30d ISO, ticketId: randomUUID() }, meta:{requestId} }. Also update apps/server/src/**tests**/routes-mounted.test.ts EXPECTED_ROUTES to add { method: DELETE, path: /api/v1/auth/me }. Add integration test in apps/server/src/routes/**tests**/auth.test.ts (RED first): unauth->401, wrong/absent confirmation->400 INVALID_CONFIRMATION, valid->202 + user.status becomes deleted + revokeAllForUser called + cookies cleared. NO shared/interface edit required for this minimal in-plan scope. FLAG: the FULL GDPR spec (persisting scheduledHardDeleteAt/ticketId and a background hard-delete-after-30d cascade job) would need a NEW migration + likely a new deletion_requests table/port => that persistence layer is packages/interfaces + migration territory and should be ISOLATED/human-gated if attempted; the route itself can return computed values reusing users.updated_at as the grace clock.
**대상 파일(힌트)**: apps/server/src/routes/auth.ts
**Acceptance**:

- GIVEN an authenticated user WHEN they DELETE /api/v1/auth/me with body {confirmation: 'DELETE_MY_ACCOUNT'} THEN respond 202 with data.scheduledHardDeleteAt (ISO, ~now+30d) and data.ticketId, set user.status='deleted', call refreshTokenFamilies.revokeAllForUser for that user, and clear the at/rt cookies
- GIVEN an authenticated user WHEN they DELETE /api/v1/auth/me with a missing or incorrect confirmation THEN respond 400 with error.code = INVALID_CONFIRMATION and leave the user status unchanged
- GIVEN a request with no valid access-token cookie WHEN it hits DELETE /api/v1/auth/me THEN respond 401 UNAUTHENTICATED
- GIVEN the route is added WHEN the routes-mounted test runs THEN EXPECTED_ROUTES includes { method: DELETE, path: /api/v1/auth/me } and the mount check passes

### P22-T1-02 — Wire periodic alert rule-evaluation loop at bootstrap (health/retention -> triggerAlert)

verdict=**confirmed-incomplete** · team=T1 · effort=M · kind=incomplete · scope=in-plan

**무엇/왜**: alert-engine ships notifiers + triggerAlert, but there is no periodic rule-evaluation loop and no app.ts/index.ts wiring; the sole triggerAlert call sits inside runRetention which itself never runs, so alerts never fire. Done = periodic health/threshold rule evaluation wired at bootstrap invoking triggerAlert.
**근거(코드베이스 검증)**: Attempted to disprove; could not. alert-engine.ts:77 defines triggerAlert; its only non-def call-sites are alert-engine.ts:107 (inside evaluateQuotaAlert) and data-retention.ts:89 (inside runRetention). grep over non-test source shows createAlertNotifier, evaluateQuotaAlert, runRetention, and health-checker.ts runHealthChecks/onUnhealthy each have ZERO references outside their own definition files, so none are ever invoked at bootstrap. app.ts (553 lines) has no alert/retention/notifier/health-eval refs; app.ts:142 /health is a static handler returning status ok with deps unknown; routes/admin.ts:79 /health/history only recent()-reads history that nothing periodically appends. index.ts (11 lines) is just createApp+serve with no scheduler. The only setInterval uses in the server are SSE heartbeats (messages.ts:578,792); the two worker files are orchestrator workers (T2), unrelated. Required interfaces already exist: packages/interfaces/src/types.ts:322/329/588/593 (HealthCheckResult, AlertEvent, HealthHistoryRepo, AlertEventRepo) and DataAccess.ts:64-65 already expose healthHistory and alertEvents, so no new shared type is needed.
**구현 지침**: Add a periodic rule-evaluation scheduler and wire it at bootstrap. (1) NEW apps/server/src/lib/alerting-scheduler.ts (T1-owned lib/) exporting startAlertingScheduler(deps) that returns a stop() handle and uses setInterval. AVOID node-cron: it is a NEW unspecified dependency; if the plan truly requires cron, isolate the task per CLAUDE.md dependency rule. Each tick: build notifier via createAlertNotifier(); call runHealthChecks(da.healthHistory, probes for db/redis/e2b/llm, onUnhealthy) where onUnhealthy triggers triggerAlert(da.alertEvents, notifier, {ruleId health-target-unhealthy, severity, message, payload result}); on a slower cadence call runRetention(da, artifactStore, {repo: da.alertEvents, notifier}). (2) apps/server/src/app.ts createApp: construct da.alertEvents (createPgAlertEventDataAccess) + da.healthHistory (createPgHealthHistoryDataAccess already imported at app.ts:62) + artifactStore, then start the scheduler; return the stop handle (or export startAlertingScheduler separately so index.ts owns lifecycle). (3) apps/server/src/index.ts: call the scheduler start after serve(), and clear the interval on SIGTERM/SIGINT for clean shutdown. (4) TDD: new test with InMemoryAlertNotifier + a failing HealthProbe (or partial retention failure) asserting notifier.sent contains the alert (RED first, since no loop exists today). NO new interface/shared type and NO API contract change needed (all repos/types already in packages/interfaces; DataAccess already exposes healthHistory+alertEvents). Route-mount guard unaffected (no new HTTP route).
**대상 파일(힌트)**: apps/server/src/lib/alert-engine.ts, apps/server/src/app.ts, apps/server/src/index.ts
**Acceptance**:

- GIVEN the server bootstrap (index.ts/createApp) WHEN the process starts THEN a periodic scheduler is registered that invokes health/threshold rule evaluation (runHealthChecks and/or runRetention) on an interval.
- GIVEN a failing health probe or a partial retention failure WHEN a scheduler tick runs THEN triggerAlert is invoked and the configured AlertNotifier.send receives the alert (verified in test with InMemoryAlertNotifier).
- GIVEN server shutdown (SIGTERM/SIGINT) WHEN the process terminates THEN the scheduler interval/timer is cleared with no leaked timers.
- GIVEN the repo WHEN `bash scripts/verify-gates.sh` runs THEN it exits 0 (typecheck/lint/tests pass, routes-mounted guard unaffected).

### P22-T1-03 — Data-retention job defined but never scheduled/invoked

verdict=**confirmed-incomplete** · team=T1 · effort=M · kind=incomplete · scope=in-plan

**무엇/왜**: runRetention (expired uploads/artifacts/share cleanup) has zero callers outside tests; index.ts registers no cron/scheduler and package.json has no cron dep, so retention never runs. Done = bootstrap scheduler invoking runRetention (its header even says cron registration is the bootstrap's job).
**근거(코드베이스 검증)**: apps/server/src/lib/data-retention.ts: runRetention is fully implemented + tested; its header (line 10) explicitly states cron registration is the bootstrap's job. grep -rn "runRetention" across the repo returns ONLY the definition (data-retention.ts:51) and its test file (data-retention.test.ts) -- zero production callers. apps/server/src/index.ts only serve()s the Hono app: no scheduler/cron/setInterval. apps/server/src/app.ts has no retention|schedule|cron|setInterval refs and no data-retention import outside tests. No cron dependency in apps/server/package.json or root package.json. Plan mandates it: rebuild_plan/12-OPS-SECURITY.md:491 ("daily 03:00 KST cron, in-process, node-cron or separate scheduled task") and rebuild_plan/08-SPRINT-PLAN.md:632 lists "data retention cron" as a T1 deliverable.
**구현 지침**: Add a bootstrap scheduler that periodically invokes runRetention. Concrete work: (1) new apps/server/src/lib/retention-scheduler.ts (T1 lib) exposing a testable startRetentionScheduler({ da, artifactStore, alerting, now?, setTimer? }) that schedules a daily run near 03:00 KST and calls runRetention(da{uploads,artifactShares}, artifactStore, alerting). Make the timer/clock injectable (default setTimeout/setInterval) so tests use a fake clock -- do NOT depend on wall-clock in tests. (2) Wire it in bootstrap: apps/server/src/index.ts calls startRetentionScheduler after serve(); either construct the deps there or have app.ts/createApp expose the already-built upload DA (createPgUploadDataAccess), artifactShare DA (createPgArtifactShareDataAccess), artifact store (createInlineArtifactStore/createS3ArtifactStore), and alert engine repo+notifier. (3) unit test the scheduler with a fake timer + spy asserting runRetention is invoked on tick and its RetentionStepResult[] propagate. NO packages/interfaces or packages/shared change required -- runRetention, DataAccess.uploads/.artifactShares, ArtifactStore.cleanupExpired, and alert-engine all already exist, so this is NOT a shared/isolated task. New dependency is avoidable: implement an in-process setInterval scheduler (plan permits "in-process ... separate scheduled task"); if node-cron is preferred instead, the plan explicitly names node-cron so it is still plan-specified. Return early/no-op the scheduler in test env to avoid leaking timers.
**대상 파일(힌트)**: apps/server/src/lib/data-retention.ts, apps/server/src/index.ts
**Acceptance**:

- GIVEN the server bootstrap (index.ts), WHEN the process starts, THEN startRetentionScheduler is invoked and a timer is registered that will run runRetention on a daily cadence (~03:00 KST), with the scheduler wired to the real upload/artifactShare DataAccess, artifact store, and alert engine.
- GIVEN the scheduler with an injected fake clock/timer, WHEN the scheduled tick fires, THEN runRetention is called exactly once with { uploads, artifactShares }, the artifact store (cleanupExpired), and alerting; a unit test asserts this via spy (RED first: currently no scheduler exists).
- GIVEN a tick where the repos contain expired uploads and expired artifact_shares, WHEN runRetention runs via the scheduler, THEN expired uploads are deleted, artifactStore.cleanupExpired runs, and expired shares are revoked, and RetentionStepResult[] is observable/logged.
- GIVEN a partial-failure tick (one step throws), WHEN runRetention completes, THEN the scheduler does not crash the process and a data-retention-failure alert is triggered (assert via alert notifier spy).

### P22-T1-04 — POST /sessions explicit session create

verdict=**confirmed-incomplete** · team=T1 · effort=S · kind=incomplete · scope=in-plan

**무엇/왜**: Contract POST /sessions (§418) returning a Session is not implemented; sessions are created lazily via ensureSession ON CONFLICT DO NOTHING upsert on first message (client-generated UUID). Deviation from contract. Done = a real create handler returning Session, or a documented contract amendment.
**근거(코드베이스 검증)**: Contract §418 (rebuild_plan/16-API-CONTRACT.md) defines POST /sessions -> 201 { data:{id,title,projectId,createdAt}, meta } (also in type table ~L1382). grep of app.post/get/patch/delete in apps/server/src/routes/sessions.ts shows NO app.post("/") — creation happens only via lazy ensureSession in apps/server/src/app.ts ~L259: `INSERT INTO sessions (id,user_id,title) VALUES ($1,$2,$3) ON CONFLICT (id) DO NOTHING`. apps/server/src/routes/messages.ts also has no post("/"). routes-mounted.test.ts EXPECTED_ROUTES omits POST /api/v1/sessions. Frontend is already half-wired: apps/web/src/hooks/useSessions.ts:145 createSession() POSTs /api/v1/sessions and reads data.{id,title,projectId,createdAt}; SessionList.tsx:411 "새 대화" button calls it; useSessions.test.ts:62 & SessionList.test.tsx:82 assert it — so the button currently 404s. No interface change required: Session type + SessionRepo.insert(Partial<T>) already exist in packages/interfaces/src/types.ts; concrete SessionsDataAccess (apps/server/src/db/session-data-access.ts, a T1-owned local interface) lacks create but is editable by T1.
**구현 지침**: Add `app.post("/")` to apps/server/src/routes/sessions.ts: parse optional { title?, projectId? } body, derive userId/org from c.get("auth") ONLY (never body — same cross-org policy as GET /), generate id via randomUUID(), insert, return 201 { data: { id, title: title??null, projectId: projectId??null, createdAt: iso }, meta: { requestId } } matching contract §418. Wire a create method into SessionsPort (routes/sessions.ts local port) and implement it in createPgSessionDataAccess (apps/server/src/db/session-data-access.ts) — reuse the existing raw INSERT pattern already used by ensureSession (SessionRepo already exposes insert(Partial<T>) in packages/interfaces, so NO shared/interface edit). Optionally validate projectId ownership before FK insert. Add { method:"POST", path:"/api/v1/sessions", phase } to EXPECTED_ROUTES in apps/server/src/**tests**/routes-mounted.test.ts (CLAUDE.md route-mount guard). NO packages/interfaces or packages/shared change needed — Session type + response shape + contract all pre-exist. RED first: a createApp-based HTTP integration test asserting 201 + Session body and cross-org 404/ownership. Frontend consumer already exists (useSessions.createSession, SessionList "새 대화" button) and needs no change once the backend answers.
**대상 파일(힌트)**: apps/server/src/routes/sessions.ts, apps/server/src/app.ts
**Acceptance**:

- GIVEN an authenticated user WHEN POST /api/v1/sessions with body {} THEN respond 201 with { data:{ id, title:null, projectId:null, createdAt }, meta:{ requestId } } and a row exists in sessions for that user (id server-generated, not client UUID)
- GIVEN body { title:'Plan', projectId:<owned> } WHEN POST /api/v1/sessions THEN 201 data.title==='Plan' and data.projectId===<owned>; a subsequent GET /api/v1/sessions lists the new session
- GIVEN a session created by user A WHEN user B (different org/user) lists sessions THEN B does not see A's session (userId derived from auth only, never request body)
- GIVEN the new POST handler WHEN routes-mounted.test.ts runs THEN POST /api/v1/sessions is in EXPECTED_ROUTES and resolves (not 404), and `pnpm test`/typecheck/lint gates pass

### P22-T1-05 — GET /sessions/:id single-session read

verdict=**confirmed-undeveloped** · team=T1 · effort=S · kind=undeveloped · scope=in-plan

**무엇/왜**: sessions.ts header claims to own GET /:id but only PATCH/DELETE exist; the bare GET /:id (contract §432) is missing, so clients cannot fetch one Session by id. Done = GET /:id returning Session with 404 existence-leak parity for cross-org.
**근거(코드베이스 검증)**: apps/server/src/routes/sessions.ts has no `app.get("/:id", ...)` handler — only patch("/:id") (L397) and delete("/:id") (L462). The header comment L1-3 claims GET/PATCH/DELETE /:id ownership; L159 comment explicitly says "단일 세그먼트 GET /:id 라우트 자체가 없음". Contract §432 (rebuild_plan/16-API-CONTRACT.md L432) defines GET /sessions/:id => {data:{id,title,projectId,createdAt,archivedAt},meta:{requestId}} with 404 for cross-org. routes-mounted.test.ts L38-44 has no GET /:id entry. Building blocks exist: SessionsPort.byId (sessions.ts L56, already used by /:id/tags L188) and SessionWithPin exposes all needed fields (db/session-data-access.ts L16-33).
**구현 지침**: Add `app.get("/:id", ...)` in apps/server/src/routes/sessions.ts. Mirror the existing ownership pattern used by /:id/tags (L185-191): derive userId from auth (c.get("auth").sub) only — never from body/query; call sessions.byId(sessionId); if !session || session.userId !== auth.sub return errorJson("NOT_FOUND", ...) 404 (existence-leak parity for cross-org). Return { data: { id, title, projectId, createdAt: session.createdAt.toISOString(), archivedAt: session.archivedAt ? session.archivedAt.toISOString() : null }, meta: { requestId: randomUUID() } } per contract §432. Register the bare /:id GET alongside other /:id routes (Hono distinguishes by segment count, so it won't shadow /:id/messages; keep static /search before it as already done). No new interface or shared type needed — SessionsPort.byId and SessionWithPin already provide everything, so NOT shared. Also: add EXPECTED_ROUTES entry { method:"GET", path:`/api/v1/sessions/${sid}` } to apps/server/src/**tests**/routes-mounted.test.ts, and add a RED-first case to apps/server/src/routes/**tests**/sessions.test.ts covering 200 (owner) and 404 (non-owner/cross-org).
**대상 파일(힌트)**: apps/server/src/routes/sessions.ts
**Acceptance**:

- GIVEN an authenticated user who owns session S, WHEN GET /api/v1/sessions/{S} is called, THEN it returns 200 with data {id,title,projectId,createdAt,archivedAt} and meta.requestId per contract §432
- GIVEN an authenticated user, WHEN GET /api/v1/sessions/{id} targets a session owned by another user/org (or a non-existent id), THEN it returns 404 NOT_FOUND with identical body in both cases (no existence leak), matching PATCH/DELETE /:id behavior
- GIVEN the route is added, WHEN the routes-mounted test runs, THEN EXPECTED_ROUTES includes GET /api/v1/sessions/:id and it is confirmed mounted (not 404-by-missing-route)
- GIVEN sibling routes /:id/messages and /:id/artifacts, WHEN GET /:id is registered, THEN those multi-segment routes still resolve correctly (no shadowing)

### P22-T1-06 — Admin new-user webhook has only a record-only dev-stub

verdict=**confirmed-incomplete** · team=T1 · effort=S · kind=incomplete · scope=in-plan

**무엇/왜**: Unlike email-sender/alert-engine (which ship real adapter + env kind-switch), webhook-dispatcher provides only createDevStubWebhookDispatcher and app.ts wires it unconditionally, so org adminWebhookUrl is non-functional at deploy — nothing to flip to. Done = a real HTTP dispatcher (Slack/Discord) + createWebhookDispatcher kind-switch.
**근거(코드베이스 검증)**: apps/server/src/lib/webhook-dispatcher.ts exports ONLY createDevStubWebhookDispatcher() (record-only, pushes to calls[], no network) — no real HTTP adapter, no createWebhookDispatcher(kind) factory. Contrast: email-sender.ts has SesEmailSender/SmtpEmailSender + createEmailSender(EMAIL_SENDER_KIND) switch (console/test/noop/ses/smtp); alert-engine.ts has SlackWebhookAlertNotifier (POST via injectable fetchImpl) + createAlertNotifier(ALERT_NOTIFIER_KIND) switch (console/test/slack). app.ts:184 wires webhookDispatcher: createDevStubWebhookDispatcher() unconditionally — nothing to flip at deploy. Plumbing IS complete: org-settings-schema.ts:86 defines adminWebhookUrl; routes/auth.ts:299-309 fire-and-forget dispatch(resolved.adminWebhookUrl,{event:'new_user',...}) on signup. grep of packages/interfaces/ for 'webhook' = 0 hits, so WebhookDispatcher interface is local to lib (no shared change).
**구현 지침**: Pure T1, mirrors alert-engine pattern; NO packages/interfaces or API-contract change (WebhookDispatcher interface is local to apps/server/src/lib/webhook-dispatcher.ts). (1) apps/server/src/lib/webhook-dispatcher.ts: add a real HttpWebhookDispatcher class implementing WebhookDispatcher.dispatch(url,payload) that POSTs JSON (Slack {text:...} / Discord {content:...} compatible body) via injectable fetchImpl = fetch (copy SlackWebhookAlertNotifier's constructor(fetchImpl) shape so it's unit-testable). Add createWebhookDispatcher(kind = process.env.WEBHOOK_DISPATCHER_KIND) switch: undefined/'devstub'/'test' -> createDevStubWebhookDispatcher() (back-compat), 'http'/'slack'/'discord' -> new HttpWebhookDispatcher(), default -> throw Unknown WEBHOOK_DISPATCHER_KIND (mirror createEmailSender/createAlertNotifier). (2) apps/server/src/app.ts:184: replace hardcoded createDevStubWebhookDispatcher() with createWebhookDispatcher() so deploy flips via env. (3) New test apps/server/src/lib/**tests**/webhook-dispatcher.test.ts: inject fake fetch, assert one POST with correct URL+new_user JSON body; assert kind-switch returns dev-stub for default and HttpWebhookDispatcher for 'http'/'slack'. Existing routes/**tests**/webhook-signup.test.ts continues to inject the dev-stub, unaffected.
**대상 파일(힌트)**: apps/server/src/lib/webhook-dispatcher.ts, apps/server/src/app.ts
**Acceptance**:

- GIVEN WEBHOOK_DISPATCHER_KIND=slack (or http) with an injected fake fetch, WHEN createWebhookDispatcher().dispatch(url, {event:'new_user',...}) is called, THEN the fake fetch receives exactly one POST to url with a JSON body containing the payload (Slack/Discord-compatible).
- GIVEN WEBHOOK_DISPATCHER_KIND unset or 'test'/'devstub', WHEN createWebhookDispatcher() is called, THEN it returns the record-only dev-stub (calls[] populated, no network) preserving existing webhook-signup.test.ts behavior.
- GIVEN an unknown WEBHOOK_DISPATCHER_KIND, WHEN createWebhookDispatcher() is called, THEN it throws 'Unknown WEBHOOK_DISPATCHER_KIND' (parity with createEmailSender/createAlertNotifier).
- GIVEN app.ts createApp() with WEBHOOK_DISPATCHER_KIND=slack env, WHEN a signup completes for an org with adminWebhookUrl set, THEN the injected fake fetch receives one new_user POST (real dispatcher is now flippable at deploy, not hardcoded to the stub).

### P22-T2-01 — Google/Gemini provider silently drops temperature and topP

verdict=**confirmed-incomplete** · team=T2 · effort=S · kind=incomplete · scope=in-plan

**무엇/왜**: Anthropic and OpenAI adapters forward temperature/topP, but llm-provider-google generationConfig sets only maxOutputTokens, so org-scoped sampling params are ignored for any gemini-* model. Provider-parity gap. Done = put temperature/topP into Gemini generationConfig with a test.
**근거(코드베이스 검증)**: llm-provider-google.ts:117 builds `generationConfig: { maxOutputTokens: input.maxTokens }` only — no reference to input.temperature/input.topP anywhere in the 199-line file. By contrast llm-provider-openai.ts:170-173 forwards temperature+top_p, and llm-provider-anthropic.ts:139-144 forwards temperature+top_p. ChatInput already declares temperature?/topP? at packages/interfaces/src/LLMProvider.ts:20-21 (no shared change needed). Gemini SDK GenerationConfig (@google/generative-ai@0.24.1, dist/types/requests.d.ts:78-79) natively supports temperature+topP. Existing test **tests**/llm-provider-google.test.ts:130 asserts only generationConfig.maxOutputTokens — temperature/topP are untested, confirming the drop.
**구현 지침**: Edit apps/server/src/orchestrator/llm-provider-google.ts (T2-owned, in orchestrator/). At line ~117 change the generationConfig assembly to conditionally include sampling params, mirroring the OpenAI adapter's spread pattern: generationConfig: { maxOutputTokens: input.maxTokens, ...(input.temperature !== undefined ? { temperature: input.temperature } : {}), ...(input.topP !== undefined ? { topP: input.topP } : {}) }. NOTE Gemini's GenerationConfig field for nucleus sampling is `topP` (camelCase, unlike OpenAI's snake_case `top_p`) — verified in @google/generative-ai@0.24.1 dist/types/requests.d.ts:78-79. NO interface/contract change: ChatInput.temperature/topP already exist in packages/interfaces/src/LLMProvider.ts:20-21, so this is NOT shared/human-gated. Add a RED-first test in apps/server/src/orchestrator/**tests**/llm-provider-google.test.ts asserting receivedRequest()?.generationConfig?.temperature and ?.topP are forwarded when set, and omitted (undefined) when not provided — reuse the existing fakeClient/receivedRequest harness (test file lines 23-61; line 130 shows the assertion pattern).
**대상 파일(힌트)**: apps/server/src/orchestrator/llm-provider-google.ts
**Acceptance**:

- GIVEN a ChatInput with temperature=0.3 and topP=0.9 WHEN google provider.chat is invoked THEN receivedRequest().generationConfig.temperature === 0.3 and .topP === 0.9 (alongside maxOutputTokens)
- GIVEN a ChatInput with temperature and topP omitted WHEN google provider.chat is invoked THEN generationConfig has neither temperature nor topP keys (undefined), preserving Gemini defaults
- GIVEN the new test written before the fix WHEN pnpm test runs THEN it fails (RED) because the current provider only sets maxOutputTokens, then passes (GREEN) after the two spreads are added
- GIVEN the change WHEN bash scripts/verify-gates.sh runs THEN typecheck+lint+test exit 0 (topP camelCase matches Gemini GenerationConfig type)

### P22-T2-02 — GET /notifications SSE notification stream

verdict=**confirmed-undeveloped** · team=T2 · effort=M · kind=undeveloped · scope=in-plan

**무엇/왜**: Contract §906 defines an SSE /notifications channel (e.g. document_indexed events for upload-modal progress, preferred over polling). No route file implements it and nothing is mounted; document indexing status is only pollable via GET /documents/:id. Done = mounted SSE route emitting NotificationEvent per the frozen union, wired to indexing/completion.
**근거(코드베이스 검증)**: No route file: find apps -iname "_notification_" returns nothing; apps/server/src/routes/ has no notifications.ts. Not mounted: grep of apps/server/src/app.ts for /notifications is empty (mounts sessions/documents/quota/admin etc., none for notifications). Not in gate: grep -c notifications apps/server/src/**tests**/routes-mounted.test.ts = 0 (absent from EXPECTED_ROUTES). No frontend consumer for this channel in apps/web/src. Only definitions exist: contract at rebuild_plan/16-API-CONTRACT.md:906 (GET /notifications SSE with document_indexed/quota_warning/alert_event/ping) and the FROZEN union NotificationEvent at rebuild_plan/14-INTERFACES.md:1063 and packages/interfaces/src/types.ts:757 (already present -> no shared-package edit needed). Reusable patterns present: streamSSE+heartbeat at apps/server/src/routes/messages.ts:574; per-key in-process pub/sub at apps/server/src/orchestrator/message-run-registry.ts (subscribeMessageRun/SubscriberQueue). Indexing is currently a synchronous dev-stub at apps/server/src/routes/documents.ts:6 (indexStatus='indexed' immediately).
**구현 지침**: Create apps/server/src/routes/notifications.ts: Hono factory returning GET / with authMiddleware, responding via streamSSE (copy 30s heartbeat/ping pattern from routes/messages.ts:574-579 to beat ALB idle timeout). Emit event:<type> + data:JSON.stringify({...fields}) per the FROZEN NotificationEvent union (packages/interfaces/src/types.ts:757) with NO envelope on SSE payloads (contract 906). Add a per-user in-process notification bus (e.g. apps/server/src/lib/notification-bus.ts or orchestrator/notification-registry.ts) modeled on message-run-registry.ts SubscriberQueue: publish(userId, event) + subscribe(userId) with unsubscribe on stream close/AbortSignal. Wire sources: document_indexed emitted where indexStatus transitions in db/document-service.ts / routes/documents.ts:6 (currently synchronous stub -> publish after index completes, scoped to owning userId); optionally quota_warning from routes/quota.ts and alert_event from lib/alert-engine.ts (MVP can start with document_indexed + ping and stub the rest). Mount in app.ts (new notificationsApp with authMiddleware, app.route('/api/v1/notifications', ...)) AND add { GET, /api/v1/notifications } to EXPECTED_ROUTES in **tests**/routes-mounted.test.ts. NO packages/interfaces edit required (NotificationEvent already frozen) -> not a shared/isolated task. Add createApp-based SSE integration test asserting text/event-stream headers + a document_indexed event after an index completes + cross-user isolation (user A must not receive user B's events).
**대상 파일(힌트)**: apps/server/src/app.ts, apps/server/src/routes/
**Acceptance**:

- GIVEN an authenticated user WHEN they GET /api/v1/notifications with Accept: text/event-stream THEN response is 200 with content-type text/event-stream and a periodic ping heartbeat (~30s) keeps the connection open
- GIVEN a subscribed user's project document finishes indexing WHEN indexStatus reaches 'indexed' THEN the stream emits event: document_indexed with data { documentId, projectId, indexStatus } matching the frozen NotificationEvent union (no envelope)
- GIVEN two users A and B each connected WHEN a document belonging to A is indexed THEN only A's stream receives the document_indexed event and B receives none (cross-user isolation)
- GIVEN the route is added WHEN the routes-mounted gate runs THEN EXPECTED_ROUTES includes GET /api/v1/notifications and the app mounts it (no 404), verified by createApp HTTP integration test

### P22-T2-03 — Redis-backed abort/resume/HITL runtime state selectable at deploy

verdict=**confirmed-incomplete** · team=T2 · effort=L · kind=incomplete · scope=in-plan

**무엇/왜**: run-registry, message-run-registry, and hitl-manager use module-level Maps; deliberate LOCAL_ONLY single-process choice, but no Redis-backed implementation exists, so cross-instance abort, SSE resume catch-up, and HITL pending approvals break under the planned multi-task ECS deploy. Done = Redis-backed adapters selectable at deploy.
**근거(코드베이스 검증)**: run-registry.ts:10 'const registry = new Map()' + abortRun():30 hold a process-local AbortController. message-run-registry.ts:57 'const runs = new Map()' (comment L1-6 says process-internal in-memory LOCAL_ONLY) with an in-process SubscriberQueue relay. hitl-manager.ts:23 'const pending = new Map()' (comment L3-5: LOCAL_ONLY, real Redis unused, replace with Redis-backed impl at deploy) with in-process settle() promise callbacks. env.ts:9 REDIS_URL is declared+validated but NEVER consumed in prod code (only test fixtures pass a fake redis://localhost:6379). No createClient / new Redis, no ioredis/redis/node-redis import or dependency anywhere in apps/server or packages. Consumers use direct module imports (routes/messages.ts:28-33, routes/sessions.ts:7, app.ts:71/297) so there is no adapter/selection seam. The plan requires Redis: 14-INTERFACES.md section 9 documents a Redis queue keyed hitl:{sessionId}:{toolCallId} with SET/DEL; sprint-plan section 0 provisions AWS Redis for ECS. So a Redis-backed multi-instance impl plus deploy-time selection is planned but absent.
**구현 지침**: Touch: apps/server/src/orchestrator/run-registry.ts, apps/server/src/orchestrator/message-run-registry.ts, apps/server/src/tools/hitl-manager.ts; wire selection in app.ts (hitlBridge at :71/:297) and consumers routes/messages.ts (:28-33) + routes/sessions.ts (:7). Add a deploy-time store-selection seam keyed on env (LOCAL_ONLY in-memory vs redis-backed), reading env.ts:9 REDIS_URL which is currently declared-but-unused. HitlBridge interface ALREADY exists in packages/interfaces section 9, so a Redis impl (e.g. tools/hitl-manager-redis.ts) implements it WITHOUT editing interfaces. For the run/message registries, define SERVER-LOCAL RunStore/MessageRunStore interfaces inside apps/server/src/orchestrator to avoid touching packages/interfaces (keeps this T2, not shared). HARD subtlety driving L effort: AbortController (run-registry), the live SubscriberQueue event relay (message-run-registry), and settle() promise callbacks (hitl-manager) are all in-process objects that CANNOT be serialized to Redis; true cross-instance abort/resume/HITL therefore requires Redis pub/sub (SUBSCRIBE/PUBLISH) to fan the abort signal + live events + resolution to whichever instance owns the live controller/subscriber/promise, plus a shared key store for pending state (hitl key structure already specified). FLAG new dependency: ioredis/node-redis is NOT in any package.json and CLAUDE.md forbids unlisted deps; the concrete npm package is not pinned by the plan (only conceptual Redis usage) -> isolate/human-gate the dependency add if not pre-approved. FLAG: does NOT strictly require editing packages/interfaces (HitlBridge already there; run/message store interfaces stay server-local); if you instead choose to formalize RunStore in packages/interfaces, that flips to team=shared and must be isolated.
**대상 파일(힌트)**: apps/server/src/orchestrator/run-registry.ts, apps/server/src/orchestrator/message-run-registry.ts, apps/server/src/tools/hitl-manager.ts
**Acceptance**:

- GIVEN two app instances sharing one Redis, WHEN a run starts on instance A and DELETE /sessions/:id/active-run is served by instance B, THEN abortRun on B fires controller.abort() for the run on A and returns cancelled=true (today B returns false because registry is process-local).
- GIVEN a message run streaming on instance A, WHEN GET /sessions/:id/messages/:messageId/stream (resume) is served by instance B, THEN B returns kind:ok with the contentSoFar catch-up snapshot and relays subsequent live events (today B returns not_found).
- GIVEN a pending HITL approval created on instance A, WHEN POST /sessions/:id/messages/hitl resolves on instance B, THEN the askApproval promise on A settles approved/denied and listPendingHitl no longer lists it (today B returns not_found).
- GIVEN env selects LOCAL_ONLY (no redis flag), WHEN the server boots single-process, THEN existing in-memory abort/resume/HITL behavior and all current tests pass unchanged (adapter selection is backward compatible).

### P22-T3-01 — knowledge_search unified retrieval over project + ephemeral chunks

verdict=**partial-refine** · team=T3 · effort=M · kind=incomplete · scope=in-plan

**무엇/왜**: knowledge-retrieval-pg loadCandidates queries project_documents only and returns ALL chunks unranked (ORDER BY chunk_index), never querying session-ephemeral chunks (EphemeralChunkRepo.hybridSearchUnified). So the knowledge_search tool cannot retrieve session-uploaded docs and pushes all ranking downstream. Done = unified retrieval over project + ephemeral chunks with pre-ranking.
**근거(코드베이스 검증)**: Contract 16-API-CONTRACT.md:513 requires knowledge_search to auto-search session+project indexes. Interface 14-INTERFACES.md:836-848 defines EphemeralChunkRepo.hybridSearchUnified as called BY knowledge_search (session ephemeral + project, RRF, SearchHit[]). But apps/server/src/knowledge/knowledge-retrieval-pg.ts:45-74 loadCandidates queries only document_chunks JOIN project_documents WHERE pd.project_id=$1; accepts sessionId but IGNORES it; never queries ephemeral_chunks; returns all rows unranked (ORDER BY document_id,chunk_index, no LIMIT). hybridSearchUnified is never implemented (grep finds only the comment at knowledge-search-handler.ts:3). Ephemeral search exists only on a separate path: db/ephemeral-chunk-search.ts searchEphemeralChunks (uploadIds-based) wired into routes/messages.ts:418 for attachment-citation injection, NOT into the agent tool, so the LLM's knowledge_search call cannot see session-uploaded docs. Ranking IS applied in-handler (knowledge-search-handler.ts:113 hybridSearch RRF), so the unranked-downstream complaint is by-design; the real defect is missing ephemeral coverage + dead sessionId. No knowledge-retrieval-pg test; knowledge-search-handler.test.ts only tests source:project.
**구현 지침**: Edit apps/server/src/knowledge/knowledge-retrieval-pg.ts loadCandidates to ALSO query ephemeral_chunks by the already-passed sessionId (JOIN uploads for filename) and merge those rows into candidates. Tag each ephemeral candidate with CitationSourceMeta {source:"ephemeral", uploadId, filename} keyed by chunk.documentId=uploadId, reusing the aliasing pattern in db/ephemeral-chunk-search.ts:72-90. The handler's existing hybridSearch (knowledge-search-handler.ts:113) then ranks the combined project+ephemeral pool, and buildCitations (citation-helper.ts:45-46) already emits uploadId meta. Scope ephemeral rows by session_id only. NO new interface/shared type needed: CitationSourceMeta already supports source "ephemeral" + uploadId, and HybridSearchCandidate is reused; stays within apps/server/src/knowledge (T3). app.ts already passes sessionId, likely no wiring change. Add RED test asserting knowledge_search returns an ephemeral-source citation for a session-uploaded chunk. Optional secondary: push topK pre-ranking into the query per hybridSearchUnified signature.
**대상 파일(힌트)**: apps/server/src/knowledge/knowledge-retrieval-pg.ts, apps/server/src/app.ts
**Acceptance**:

- GIVEN a session with an uploaded doc indexed into ephemeral_chunks and no project docs, WHEN the agent invokes knowledge_search with a matching query, THEN the tool returns at least one citation with source="ephemeral" and the correct uploadId/filename.
- GIVEN a session whose project also has document_chunks, WHEN knowledge_search runs, THEN candidates from BOTH project_documents and ephemeral_chunks are merged and RRF-ranked together (a more-relevant ephemeral chunk can outrank a project chunk).
- GIVEN loadCandidates is called with a sessionId that has no ephemeral_chunks, WHEN it runs, THEN behavior is unchanged (project-only candidates, no error).
- GIVEN the new behavior, WHEN the RED test runs before implementation, THEN it fails because loadCandidates ignores sessionId and returns zero ephemeral candidates.

### P22-T3-02 — Serve project-document CRUD off the nested contract path /projects/:id/documents*

verdict=**partial-refine** · team=T3 · effort=M · kind=incomplete · scope=in-plan

**무엇/왜**: Contract nests document CRUD under /projects/:id/documents* (§666-710) but the implementation is mounted at flat /api/v1/documents with ?projectId query. Functionally complete but path-shape deviation from contract. Done = serve the nested contract URLs (or amend contract).
**근거(코드베이스 검증)**: apps/server/src/app.ts:385-397 mounts createDocumentRoutes at flat /api/v1/documents. apps/server/src/routes/documents.ts serves GET / reading c.req.query('projectId') (L86), POST / reading projectId from form body (L119), GET /:id, POST /:id/retry, DELETE /:id — projectId is never in the path. Contract rebuild_plan/16-API-CONTRACT.md §666-710 (section 5 Project Documents) requires nested POST|GET /projects/:id/documents, GET /projects/:id/documents/:docId, POST .../:docId/retry, DELETE .../:docId. grep for 'projects/:id/documents' and ':projectId/documents' across apps/server/src returned empty; routes/projects.ts has zero document references — no nested wiring exists. Flat shape is hardcoded in apps/server/src/**tests**/routes-mounted.test.ts:85-90 and web client apps/web/src/hooks/useDocuments.ts:47,73,98. Feature is functionally complete (perms enforced by db/document-service.ts); only the URL shape deviates from contract.
**구현 지침**: Two acceptable resolutions per the gap's Done criteria: (a) serve the nested contract URLs, or (b) amend the contract to bless the flat shape. Recommend (a). Files to touch: apps/server/src/app.ts (change mount from /api/v1/documents to /api/v1/projects/:id/documents, or add a nested sub-app), and apps/server/src/routes/documents.ts (read projectId from the path param instead of ?projectId query on GET / and from the form body on POST /). CAUTION param-name collision: the project id and document id would both be ':id'; rename the doc param to ':docId' so routes become GET /, POST /, GET /:docId, POST /:docId/retry, DELETE /:docId under the projects/:id/documents prefix with :id bound by the parent mount. Must also update the guard test EXPECTED_ROUTES in apps/server/src/**tests**/routes-mounted.test.ts:85-90 (currently asserts flat /api/v1/documents shape) and, if flat routes are removed rather than kept for back-compat, update the T6 web client apps/web/src/hooks/useDocuments.ts:47,73,98 plus its test apps/web/src/hooks/**tests**/useDocuments.test.ts (this crosses into T6 — safest to KEEP flat routes as back-compat aliases and ADD nested, avoiding a coordinated T3+T6 change). NO packages/interfaces or packages/shared change needed — ProjectDocumentRecord DTO already exists (imported in documents.ts), so this is NOT a shared/human-gated task. Separate deliberate dev-stub deviations to NOT conflate with this gap: POST returns 201 + full DTO indexStatus='indexed' vs contract 202 {documentId, indexStatus:'pending'}, and GET /:id omits progress:{stage,percent,etaSec}.
**대상 파일(힌트)**: apps/server/src/routes/documents.ts, apps/server/src/app.ts
**Acceptance**:

- GIVEN an authed member of a project WHEN they POST multipart to /api/v1/projects/:id/documents THEN the document is created and returned (contract nested path resolves, not 404)
- GIVEN an indexed document WHEN GET /api/v1/projects/:id/documents/:docId is called THEN it returns that document's DTO scoped to the path projectId
- GIVEN the routes-mounted guard test WHEN the suite runs THEN EXPECTED_ROUTES asserts the nested /projects/:id/documents* paths resolve (non-404) and the flat-path assertions are updated or retained as back-compat
- GIVEN a cross-org or non-member actor WHEN they hit any nested /projects/:id/documents* endpoint THEN existence-leak protection still maps FORBIDDEN/NOT_FOUND to 404 (behavior preserved from db/document-service.ts)

### P22-T3-03 — Ephemeral (attachment) indexing ignores org chunk settings

verdict=**confirmed-incomplete** · team=T3 · effort=M · kind=incomplete · scope=in-plan

**무엇/왜**: indexEphemeralUpload accepts chunkOptions but the uploads indexing path (app.ts, upload-service.ts) never resolves org settings, so session-attachment RAG chunking always uses DEFAULT 800/100 regardless of org config. Done = per-request settings.resolve threaded into ephemeral-indexer chunkOptions.
**근거(코드베이스 검증)**: app.ts:373-378 wires uploads `indexing` as a STATIC object {parserPipeline, embeddingProvider, bulkInsert, logger} with no chunkOptions and no settings resolver. routes/uploads.ts:65-104 calls settings.resolve(auth.org) but only reads allowedUploadExtensions/maxUploadSizeMb/maxUploadCount, never ragChunkSizeTokens/ragChunkOverlapTokens. db/upload-service.ts:70-79 passes the static `indexing` straight into indexEphemeralUpload, so chunkOptions is always undefined. ephemeral-indexer.ts:64 -> chunker.ts:36-37 then falls back to DEFAULT 800/100. Contrast db/document-service.ts:66-83,197,277 which correctly resolves per-org chunk config via resolveChunkOptions(settings, orgId) -> resolved.ragChunkSizeTokens/ragChunkOverlapTokens. upload-service.test.ts:151,175 only assert chunks get produced/bulk-inserted, never per-org sizing, so no guard exists. The chunkOptions hook exists but is never fed on the ephemeral path.
**구현 지침**: No interface/shared-type change needed (ChunkOptions and EphemeralIndexerDeps.chunkOptions already exist, so team is T3 not "shared"). Replicate the document-service.ts resolveChunkOptions pattern on the ephemeral path. (1) apps/server/src/routes/uploads.ts: extend the existing deps.settings.resolve(auth.org) block (already fetched for whitelist/size/count) to also derive {chunkSizeTokens: resolved.ragChunkSizeTokens, overlapTokens: resolved.ragChunkOverlapTokens} and pass it into service.createUpload. (2) apps/server/src/db/upload-service.ts: thread a per-request chunkOptions override through CreateUploadInput (or a new param) -> indexForSessionIfNeeded -> indexEphemeralUpload deps, overriding static indexing.chunkOptions; keep the fail-soft try/catch and also cover the sha256-dedup re-index branch (createUpload lines 99-104 and 121). Design wrinkle: createUpload's actor only carries userId, so orgId-derived chunkOptions must originate in the route layer (auth.org) rather than the service. Reuse the same resolved settings object already fetched in the route to avoid a second settings.resolve call.
**대상 파일(힌트)**: apps/server/src/app.ts, apps/server/src/db/upload-service.ts, apps/server/src/knowledge/ephemeral-indexer.ts
**Acceptance**:

- GIVEN an org with org_settings.ragChunkSizeTokens/ragChunkOverlapTokens set to non-default values, WHEN a user uploads a session attachment (POST /api/v1/uploads with sessionId), THEN the produced ephemeral_chunks reflect the org's chunk size/overlap, not the DEFAULT 800/100.
- GIVEN no settings resolver injected (or resolve throws), WHEN an attachment is indexed, THEN chunking fails soft to DEFAULT_ORG_SETTINGS values and the upload still succeeds (existing fail-soft behavior preserved).
- GIVEN the same bytes re-attached to a different session (sha256 dedup re-index branch), WHEN re-indexing runs, THEN the per-org chunkOptions are applied on that path too.
- GIVEN a RED test asserting org-specific chunk boundaries on the ephemeral path, WHEN run before the fix, THEN it fails because chunkOptions is undefined; after wiring, it passes and full test/typecheck gates stay green.

### P22-T3-04 — Org-scoped RAG chunk settings not applied at project-document indexing (resolver never injected)

verdict=**confirmed-incomplete** · team=T3 · effort=S · kind=incomplete · scope=in-plan

**무엇/왜**: document-service.ts has ChunkSettingsResolverPort/resolveChunkOptions but no production caller injects a resolver: app.ts createDocumentRoutes omits settings:settingsService and routes/documents.ts drops deps.settings, so admin ragChunkSizeTokens/Overlap are stored+validated yet always fall back to DEFAULT 800/100. ~2-line wiring gap. Done = inject settingsService end-to-end so a 1200-token org setting actually splits at 1200 (P15-T3-01 acceptance).
**근거(코드베이스 검증)**: db/document-service.ts:34-49 defines ChunkSettingsResolverPort + DocumentIndexingDeps.settings?; resolveChunkOptions (66-83) is called in indexDocument (197-201) and retryDocument (277-281), fail-soft to chunkText defaults. BUT routes/documents.ts:54-63 (createDocumentRoutes) constructs the indexing-deps object forwarding ONLY parserPipeline+embeddingProvider, dropping deps.settings; and app.ts:389-395 calls createDocumentRoutes WITHOUT settings:settingsService (settingsService exists at app.ts:163 and is passed as `settings` to other routes at 181/249/282/380, structurally compatible: resolve(orgId)=>ResolvedOrgSettings). Unit test db/**tests**/document-service.test.ts:477-505 proves service works when settings injected directly (1200 splits at 1200), but integration test **tests**/integration/documents-composition.test.ts has zero settings/1200/createDocumentRoutes coverage — so end-to-end wiring is non-functional: admin ragChunkSizeTokens/ragChunkOverlapTokens (org-settings-schema.ts:36-37,110-111) always fall back to chunker.ts DEFAULT 800/100. No packages/interfaces or packages/shared edit needed.
**구현 지침**: Two-point wiring fix, no new types (ChunkSettingsResolverPort, DocumentIndexingDeps, ResolvedOrgSettings, SettingsService all already exist -> NOT shared, no interface/contract change). (1) apps/server/src/routes/documents.ts:57-62 — in createDocumentRoutes, add `settings: deps.settings` to the indexing-deps object built when parserPipeline && embeddingProvider are present (currently only forwards parserPipeline+embeddingProvider, silently dropping the resolver deps type Partial<DocumentIndexingDeps> already accepts). (2) apps/server/src/app.ts:389-395 — add `settings: settingsService,` to the createDocumentRoutes({...}) call (settingsService already instantiated at app.ts:163, structurally satisfies ChunkSettingsResolverPort since SettingsService.resolve returns full ResolvedOrgSettings). (3) TDD RED first: add a createApp-based integration test (natural home apps/server/src/**tests**/integration/documents-composition.test.ts) that sets an org's ragChunkSizeTokens (via admin-settings service/route or seeded org_settings) then POST /api/v1/documents a large doc and asserts chunkCount/chunk sizing reflects the org value, plus a default-when-unset case and settings.resolve-throws fail-soft case. Mirror the existing unit test at db/**tests**/document-service.test.ts:477-505 but at the createApp/HTTP composition layer to close the untested wiring seam.
**대상 파일(힌트)**: apps/server/src/app.ts, apps/server/src/routes/documents.ts, apps/server/src/db/document-service.ts
**Acceptance**:

- GIVEN an org with ragChunkSizeTokens=1200 set via admin settings WHEN a project document large enough to exceed 1200 tokens is uploaded via POST /api/v1/documents (through createApp, real wiring) THEN it is split at ~1200-token chunks (chunkCount matches 1200 sizing), not the DEFAULT 800.
- GIVEN an org with no ragChunkSizeTokens override WHEN a document is indexed THEN chunking uses DEFAULT_ORG_SETTINGS 800/100 (backward-compatible default).
- GIVEN the injected settings resolver throws at resolve() WHEN a document is indexed THEN indexing still succeeds fail-soft at the 800/100 defaults (indexStatus='indexed', not 'failed').
- GIVEN the createApp document route wiring WHEN inspected/tested THEN settingsService is threaded app.ts -> createDocumentRoutes -> createDocumentService (indexing.settings defined), verified by an integration/composition test that currently fails RED before the wiring is added.

### P22-T4-01 — artifact_create routes large/binary content to S3 ArtifactStore (storageKind=s3)

verdict=**confirmed-incomplete** · team=T4 · effort=M · kind=incomplete · scope=in-plan

**무엇/왜**: artifact-create-handler handles only inline-stored text-type artifacts; large/binary artifact storage routing (S3, 256KB auto-split path) is explicitly out of scope in the handler. Done = handler routes large/binary artifacts to the s3 ArtifactStore branch (storageKind=s3).
**근거(코드베이스 검증)**: artifact-create-handler.ts:5 explicitly declares S3/large-file routing out of scope; inputSchema (line 44) accepts only content:string; lines 83-92 call service.createArtifact WITHOUT s3Key and with no try/catch. artifact-service.ts:54-60 computes storageKind via decideStorageKind and THROWS INVALID_INPUT when storageKind==='s3' && !s3Key — so any content >=256_000 bytes crashes out of invoke. assemble-builtin-tools.ts:140 wires the handler with only {da} (no ArtifactStore), while its deps already carry objectStore (line 51). Supporting pieces exist but are unused by the create path: createS3ArtifactStore (lib/artifact-store.s3.ts), CreateArtifactInput.s3Key + decideStorageKind (artifact-service.ts:11-27), and S3 read/download wiring (routes/artifacts.ts:67-127, app.ts:404-410). No tool handler calls ArtifactStore.put() for artifacts. The handler test (**tests**/artifact-create-handler.test.ts) only covers small inline content — no s3 case.
**구현 지침**: Edit apps/server/src/tools/handlers/artifact-create-handler.ts: extend ArtifactCreateToolDeps to accept an ArtifactStore (s3Store: ArtifactStore) — reuse existing packages/interfaces ArtifactStore (put/get), no new shared types needed. In invoke(): compute the data Buffer, and when sizeBytes >= INLINE_STORAGE_THRESHOLD_BYTES (import decideStorageKind/INLINE_STORAGE_THRESHOLD_BYTES from db/artifact-service.ts), first allocate an id or call s3Store.put({artifactId, content, sizeBytes, mimeType}) to upload, then pass the returned locator as s3Key into service.createArtifact so the row is created with storageKind='s3'. Note current artifact-service.put ordering: createArtifact requires s3Key BEFORE the row exists but S3 key is `artifacts/${artifactId}` (chicken-egg) — resolve by generating the artifact id up-front or by adding a create-then-upload-then-update path (prefer keying on a pre-generated UUID; keep decideStorageKind as the single source of the 256KB threshold). Wire the store in apps/server/src/tools/assemble-builtin-tools.ts:140 by constructing createS3ArtifactStore(deps.objectStore) (objectStore already in deps) and passing it to createArtifactCreateTool. Add binary/base64 handling if desired but the load-bearing done-criterion is large-text -> storageKind='s3'. Add unit tests in tools/handlers/**tests**/artifact-create-handler.test.ts (large content -> s3 put called + storageKind='s3', small content -> inline unchanged) and optionally an integration test through routes/artifacts.ts for presigned download. NO packages/interfaces or packages/shared edit required (ArtifactStore, CreateArtifactInput.s3Key, decideStorageKind all pre-exist) — stays T4/apps/server. File-location caveat: handler lives under tools/handlers (not strictly T4's listed path), but domain is artifacts.
**대상 파일(힌트)**: apps/server/src/tools/handlers/artifact-create-handler.ts
**Acceptance**:

- GIVEN artifact_create is invoked with content whose UTF-8 byte length >= 256_000, WHEN the tool runs, THEN it uploads via s3Store.put() and service.createArtifact receives a non-empty s3Key, producing an ArtifactRecord with storageKind='s3' and inlineContent=null (and NOT throwing INVALID_INPUT).
- GIVEN artifact_create is invoked with small content (< 256_000 bytes), WHEN the tool runs, THEN the artifact is stored inline (storageKind='inline', s3Key=null) exactly as today — no regression in the existing inline test.
- GIVEN a large (s3) artifact was created by the tool, WHEN a client calls GET /api/v1/artifacts/:id then GET /api/v1/artifacts/:id/content with the returned exp+sig token, THEN the original bytes stream back with a valid content-disposition and the size matches sizeBytes.
- GIVEN the assemble-builtin-tools wiring, WHEN assembleBuiltinTools runs with objectStore present, THEN createArtifactCreateTool is constructed with an s3Store dependency (assert via injection/spy) rather than {da} only.

### P22-T4-02 — Public share 410 cannot distinguish expired vs revoked

verdict=**confirmed-incomplete** · team=T4 · effort=M · kind=incomplete · scope=in-plan

**무엇/왜**: artifact-share-service lumps revokedAt||expiresAt<=now into a single GONE; public-share maps to 410 with no reason field, so the front-end (SharePublicView/ConversationSharePublicView) shows the same screen for both. Cross-team: needs a T1 server change adding a reason (expired|revoked) to the 410 before T6 can render clock-vs-❌ (P17-T6-07). Done = server emits reason, UI branches.
**근거(코드베이스 검증)**: Server lumps both states into one GONE with no reason. artifact-share-service.ts:81 `if (found.revokedAt || found.expiresAt.getTime() <= Date.now())` throws a single GONE; conversation-share-service.ts:157-165 is identical. Routes map GONE to 410 via errorJson(code,message) producing {error:{code,category,message,retryable}} with no reason field: public-share.ts:64,83 and conversation-share.ts:115. UI shows one screen for both: SharePublicView.tsx lines 4-5 comment explicitly says the API does not distinguish the two, and lines 47-63 render a single Clock 410 screen; ConversationSharePublicView.tsx:6-7,80-96 identical. Hooks discard the body: useShare.ts:46-49 and useConversationShare.ts:53-54 only set a boolean `gone`. grep for 'reason' returns empty across all six share files. packages/shared/src contains only index.ts + a hello test (no Zod enforcement), so the 410 body is built ad-hoc locally and the error code union is local to the T4 service file -- no packages/interfaces or packages/shared edit required. Tests only assert status: public-share.test.ts:167 (expired) and :203 (revoked) both assert 410 with no reason check.
**구현 지침**: Cross-team T4(server) + T6(UI). NO packages/interfaces or packages/shared change needed (error code/reason built ad-hoc via local errorJson; ArtifactShareServiceError and ConversationShareServiceError code unions are local to their service files) -- do NOT isolate as shared. SERVER (T4-owned per 07-AGENT-TEAMS:17): (1) apps/server/src/db/artifact-share-service.ts:81 -- split the lumped condition and carry a reason='revoked' when found.revokedAt is truthy else 'expired'; simplest is to add a `reason` property to ArtifactShareServiceError (or add distinct codes EXPIRED/REVOKED to the local union). (2) apps/server/src/db/conversation-share-service.ts:157-165 -- same split. (3) apps/server/src/routes/public-share.ts:60-68,79-87 and apps/server/src/routes/conversation-share.ts:111-119 -- include the reason in the 410 error body (e.g. errorJson(code,message,reason) adding reason to the error object). UI (T6-owned, apps/web/src/): (4) apps/web/src/hooks/useShare.ts:46-49 and useConversationShare.ts:53-54 -- on 410 read res.json() and capture reason into state (add goneReason:'expired'|'revoked'|null). (5) apps/web/src/components/share/SharePublicView.tsx:47-63 and ConversationSharePublicView.tsx:80-96 -- branch icon/copy: Clock for expired, an X/ban icon (e.g. lucide Ban/XCircle, accent color) for revoked (P17-T6-07). Update SharePublicView.test.tsx and ConversationSharePublicView.test.tsx plus public-share.test.ts to assert the reason. Server portion verifiable by createApp HTTP integration test; UI portion verifiable by existing RTL/jsdom component tests -- no real browser needed.
**대상 파일(힌트)**: apps/server/src/db/artifact-share-service.ts, apps/server/src/routes/public-share.ts, apps/web/src/components/share/SharePublicView.tsx
**Acceptance**:

- GIVEN a share whose expiresAt is in the past WHEN GET /api/v1/share/:token is called THEN response is 410 and the error body carries reason='expired'
- GIVEN a share with revokedAt set WHEN GET /api/v1/share/:token (and the conversation-shares equivalent) is called THEN response is 410 and the error body carries reason='revoked'
- GIVEN the 410 body reason='expired' WHEN SharePublicView/ConversationSharePublicView render THEN a clock icon and expired copy show; GIVEN reason='revoked' THEN a distinct X/ban icon and revoked copy show (RTL component test asserts the two branches differ)
- GIVEN an existing valid share WHEN fetched THEN it still returns 200 with data unchanged (no regression in the happy path)

## Tier 2 — Open WebUI 파리티 (계약 변경 불필요, 즉시 빌드)

| id          | team | eff | browser | contract | title                                                        |
| ----------- | ---- | --- | ------- | -------- | ------------------------------------------------------------ |
| `P22-T1-07` | T1   | M   | ✅      | ·        | Group card lacks per-resource access-grant toggle (no…       |
| `P22-T1-08` | T1   | L   | ✅      | ·        | No image-generation feature (backend tool + in-chat trigger… |
| `P22-T1-09` | T1   | M   | ·       | ·        | No #url website loader (fetch_url tool) into chat            |
| `P22-T3-05` | T3   | M   | ·       | ·        | No cross-encoder reranking stage in RAG                      |
| `P22-T6-01` | T6   | M   | ✅      | ·        | No clone/duplicate chat action                               |
| `P22-T6-02` | T6   | M   | ✅      | ·        | Admin Connectors allowedTools is read-only (no editable PUT) |
| `P22-T6-03` | T6   | S   | ·       | ·        | No UI to delete/remove an indexed knowledge document         |
| `P22-T6-04` | T6   | S   | ✅      | ·        | Images shown only as filename chips, no thumbnail/preview    |
| `P22-T6-05` | T6   | M   | ✅      | ·        | Queue messages while a response is generating                |
| `P22-T6-06` | T6   | L   | ✅      | ·        | No multi-model side-by-side comparison chat                  |
| `P22-T6-07` | T6   | M   | ✅      | ·        | No PWA / installable app manifest                            |
| `P22-T6-08` | T6   | M   | ✅      | ·        | Speech-to-Text voice input                                   |
| `P22-T6-09` | T6   | M   | ✅      | ·        | Text-to-Speech read-aloud on messages                        |

### P22-T1-07 — Group card lacks per-resource access-grant toggle (no list-grants-for-group API)

verdict=**confirmed-incomplete** · team=T1 · effort=M · kind=incomplete · scope=candidate · **★needsBrowser**

**무엇/왜**: GroupsManager supports group CRUD + membership, but the per-resource grant toggle in the group card is scoped out (no 'list grants for group' query API). A standalone GrantsManager exists but group-level grant wiring is missing. Done = list-grants-for-group API + in-card grant binding.
**근거(코드베이스 검증)**: GroupsManager.tsx (apps/web/src/components/admin/GroupsManager.tsx) lines 5-11 explicitly document the per-resource grant toggle as deliberately scoped-out, noting the DB layer only supports grantsForResource and that a follow-on T1 route (list/grant/revoke) must be added first; the group card body renders only rename/delete/member-chips/add-member input with no grant UI. resource-grants-data-access.ts (apps/server/src/db/) ResourceGrantsDataAccess interface exposes grant, grantsForResource, grantsForResources, groupIdsForUser, revoke but NO grantsForSubject/grantsForGroup. admin-grants.ts (apps/server/src/routes/) GET / uses ResourceQuerySchema (resourceType+resourceId only, calls grantsForResource); subjectType/subjectId appear only in POST/DELETE, never in the GET listing, so there is no group-scoped list endpoint. GrantsManager.tsx is resource-first only. grep for grant in packages/interfaces and packages/shared returned empty, so the interface is local and no shared-type edit is needed. admin-grants mounted at app.ts:522; routes-mounted.test.ts lines 139/142/145 cover only resource-scoped GET/POST/DELETE.
**구현 지침**: Two-part; NO packages/interfaces or packages/shared edits (NOT isolate/human-gated -- ResourceGrantsDataAccess is local to apps/server/src/db/resource-grants-data-access.ts, GrantDto is local to apps/web/src/hooks/useGrants.ts). BACKEND (T1, load-bearing blocker): (1) Add grantsForSubject(orgId, subjectType, subjectId) to ResourceGrantsDataAccess interface + pg impl returning rows of {resourceType, resourceId, access} via SELECT resource_type, resource_id, access FROM resource_grants WHERE org_id=$1 AND subject_type=$2 AND subject_id=$3, following the explicit org_id WHERE double-defense convention of the sibling methods. (2) Extend GET /api/v1/admin/grants in apps/server/src/routes/admin-grants.ts to accept an alternate subject-scoped query (subjectType=group and subjectId=groupId -> grantsForSubject) alongside the existing resource-scoped query; keep admin-only guard; add integration test + a routes-mounted.test.ts entry for the new query shape. FRONTEND (T6 follow-on, apps/web/src/): add loadGrantsForGroup to useGrants.ts (or a new hook) consuming the subject-scoped GET, then wire an in-card grant-binding section into GroupsManager.tsx group card that lists the group's current grants and offers grant/revoke controls (POST/DELETE /api/v1/admin/grants with subjectType=group), refreshing on mutation. Cross-org isolation must be preserved (orgId from JWT only, 404/empty for out-of-org groupId).
**대상 파일(힌트)**: apps/web/src/components/admin/GroupsManager.tsx, apps/server/src/routes/admin-grants.ts
**Acceptance**:

- GIVEN an admin authenticated in org A WHEN they call GET /api/v1/admin/grants?subjectType=group&subjectId=<groupId> THEN the response lists every resource grant held by that group (resourceType, resourceId, access) scoped to org A, and an out-of-org groupId yields empty/404
- GIVEN the new grantsForSubject(orgId, subjectType, subjectId) data-access method WHEN invoked for a group holding 2 grants THEN it returns exactly those 2 rows and a unit test proves org isolation via the explicit WHERE org_id clause
- GIVEN the admin panel Groups page in a browser WHEN an admin opens a group card THEN it displays that group's current resource grants and provides an in-card control to grant and revoke a resource (POST/DELETE /api/v1/admin/grants with subjectType=group), with the list refreshing after each mutation
- GIVEN the new route query shape WHEN pnpm test runs routes-mounted + admin-grants integration tests THEN the subject-scoped GET is covered and the existing resource-scoped GET/POST/DELETE still pass with no regression

### P22-T1-08 — No image-generation feature (backend tool + in-chat trigger + settings)

verdict=**confirmed-undeveloped** · team=T1 · effort=L · kind=undeveloped · scope=candidate · **★needsBrowser**

**무엇/왜**: No image-generation UI or backend anywhere in the web app (grep for image.generation/dall-e = 0). Open WebUI ships multi-engine text-to-image + prompt editing. Plan doesn't schedule it. Done (if adopted) = an image-gen tool + in-chat trigger + settings; otherwise mark out-of-scope.
**근거(코드베이스 검증)**: Only 2 grep hits for image-gen exist and both are FAKE distractor tools in test fixtures: apps/server/src/tools/**tests**/tool-router.test.ts:58 and apps/server/src/routes/**tests**/messages.test.ts:1235 both call makeTool(image_generate, generate an image from a text prompt using diffusion) with a stub invoke returning unused, purely to test tool-router relevance filtering. No real handler: apps/server/src/tools/handlers/ has only artifact-create, code-interpreter, deep-research, knowledge-search, memory-tool, search-chats, web-search; assemble-builtin-tools.ts wires exactly those and no image tool. No route: apps/server/src/routes/ (26 files) has none for image/media gen. Zero hits in packages/interfaces, packages/shared, rebuild_plan/16-API-CONTRACT.md, rebuild_plan/08-SPRINT-PLAN.md, all of rebuild_plan/, and apps/web/src (ChatInput.tsx image-adjacent code is file upload/attachments via useAttachments, not generation). Feature is genuinely absent and not scheduled in the plan.
**구현 지침**: Build a new builtin AgentTool image_generate, mirroring the existing web_search pattern. Files: (1) NEW apps/server/src/tools/image-gen-port.ts (ImageGenPort interface: generate(prompt,size,n) returns image bytes/url, following web-search-port.ts). (2) NEW apps/server/src/tools/image-gen-provider-dev-stub.ts for LOCAL_ONLY (returns a placeholder image; mirror web-search-provider-dev-stub.ts). (3) NEW apps/server/src/tools/handlers/image-generate-handler.ts createImageGenerateTool(): invoke calls port, stores result via deps.objectStore + artifactDa as artifact of kind image, returns AgentToolResult kind:file artifactId. NO shared-type change needed: artifactKind image already at packages/interfaces/src/types.ts:220 and AgentToolResult file content at line 652, plus existing lib/object-store.ts. (4) Wire into apps/server/src/tools/assemble-builtin-tools.ts (add imageGenPort dep + push tool; real provider gated on an API key, dev-stub fallback like tavily/e2b). (5) Add imageGenEnabled org flag in apps/server/src/lib/settings-service.ts (DEFAULT_ORG_SETTINGS is here, NOT in shared) and gate assembly on it. (6) T6 follow-up: admin settings toggle in apps/web/src (admin-settings) + verify in-chat rendering of the returned image artifact via existing ToolCallRenderer/artifact path. FLAG: real external provider (DALL-E/OpenAI images/ComfyUI) is an UNSPECIFIED dependency per CLAUDE.md hard rules; ship dev-stub only and human-gate/isolate the real adapter. No packages/interfaces or packages/shared edit required, so NOT shared.
**대상 파일(힌트)**: apps/web/src/components/chat/
**Acceptance**:

- GIVEN an org with imageGenEnabled=true and the image_generate tool assembled, WHEN a user asks the chat to generate an image, THEN the model invokes image_generate, the (dev-stub) provider returns an image, it is stored as an artifact of kind image, and the tool returns AgentToolResult kind:file with that artifactId
- GIVEN imageGenEnabled=false (or the flag absent), WHEN messages tools are assembled, THEN image_generate is NOT present in the tool list (assembly gate test, mirroring knowledge_search optional gate)
- GIVEN the returned image artifact, WHEN the assistant message renders in the browser chat UI, THEN the generated image is displayed inline (browser check of ToolCallRenderer/artifact rendering)
- GIVEN an org admin in the settings panel, WHEN they toggle Image generation on/off, THEN the imageGenEnabled setting persists and controls whether the tool is offered (browser check of admin settings)

### P22-T1-09 — No #url website loader (fetch_url tool) into chat

verdict=**confirmed-undeveloped** · team=T1 · effort=M · kind=undeveloped · scope=candidate

**무엇/왜**: web_search exists, but there is no '#'+URL loader that fetches, cleans and injects a specific web page into the conversation. Common in Open WebUI. Done (if adopted) = a fetch_url/URL-attach path with SSRF validation and citation.
**근거(코드베이스 검증)**: Searched the full tool surface. apps/server/src/tools/assemble-builtin-tools.ts assembles ONLY: artifact_create, web_search, code_interpreter, search_chats, view_chat, add_memory, search_memories, deep_research, knowledge_search — no url/page fetch tool. grep for name:"web_fetch"/"fetch_url"/"read_url" over apps/server/src + apps/web/src = zero handlers; only handler in tools/handlers matching web is web-search-handler.ts (returns Tavily search-result LISTS, not a page fetch). web_fetch appears ONLY as (a) an aspirational comment in apps/server/src/mcp/url-validator.ts:2 ("...+ web_fetch 도구 호출 시 공통으로 사용") and (b) an example log string in apps/server/src/lib/**tests**/logger.test.ts:96 — neither is an implementation. The `#` trigger in apps/web/src/components/chat/ChatInput.tsx:90-113 (P20-T6-09) resolves to type "document", whose entities (lines 223-229) are already-UPLOADED attachments (items with uploadId), NOT arbitrary URLs — so `#`+URL does not fetch/clean/inject a page. grep for fetch_url/read_url/web_fetch/url_load in packages/interfaces, rebuild_plan/14-INTERFACES.md, rebuild_plan/16-API-CONTRACT.md returned nothing. No arbitrary-URL fetch() in tools/knowledge/lib beyond the MCP client pool and Tavily provider. SSRF validation ALREADY exists and is reusable: validateMcpUrl in apps/server/src/mcp/url-validator.ts. Citation pattern already exists: web-search-handler.ts imports Citation from knowledge/citation-helper.js.
**구현 지침**: Add a new agent tool `web_fetch` (or `fetch_url`) as a server-side handler, modeled directly on apps/server/src/tools/handlers/web-search-handler.ts. Concrete touchpoints (all within T1 ownership — no shared-type change needed): 1) NEW apps/server/src/tools/handlers/web-fetch-handler.ts exporting createWebFetchTool(): AgentToolSpec { name:"web_fetch", inputSchema {url:string}, permissionTier:"tool", defaultPolicy:"allow" or "ask", tags:["read-only","web"] } + invoke that (a) runs validateMcpUrl from ../../mcp/url-validator.js FIRST (SSRF: RFC-1918 block + DNS-rebind IP resolve — bind fetch to resolvedIps), (b) HTTP GET with size/time limits + content-type guard, (c) extracts/cleans main text, (d) returns tool_result JSON { url, title, content } plus a Citation (reuse knowledge/citation-helper.ts, same as web-search-handler). 2) Wire it into apps/server/src/tools/assemble-builtin-tools.ts return array (add to the AgentTool[] and to deep_research workerTools if read-only research should reach it). 3) Tests: web-fetch-handler.test.ts (public URL happy path + SSRF INTERNAL_IP_FORBIDDEN reject path with a fake resolveHostname) and an assembly test asserting web_fetch is present in assembleBuiltinTools output (L1 last-mile). NOTE: web-search-port.ts precedent confirms tool ports live in apps/server, NOT packages/interfaces — AgentTool/AgentToolSpec/ToolContext already exist in @wchat/interfaces, so NO shared/interface change (stays T1). FLAG — the "clean" (HTML->readable text) step: no cheerio/readability/html-to-text/jsdom is in apps/server/package.json. A minimal regex/tag-strip clean avoids a new dependency; if a real readability lib is wanted it is an UNSPECIFIED dependency and per CLAUDE.md must be isolated/human-gated (record in .ralph/blocked_tasks) rather than added silently. Optional follow-on (T6, separate task/group, browser-verified): teach the `#` composer trigger in apps/web/src/components/chat/ChatInput.tsx to recognize a pasted URL after `#` and attach it as a fetch target — not required for the core done-criteria.
**대상 파일(힌트)**: apps/server/src/tools/
**Acceptance**:

- GIVEN a reachable public URL, WHEN the model invokes web_fetch with that url, THEN the handler fetches the page, extracts cleaned main text, and returns tool_result JSON containing {url,title,content} plus a Citation (title+url) following the web-search-handler/citation-helper pattern.
- GIVEN an internal/link-local URL (e.g. http://10.0.0.1 or http://169.254.169.254), WHEN web_fetch is invoked, THEN validateMcpUrl rejects it (INTERNAL_IP_FORBIDDEN) BEFORE any network call and the tool returns a WChatError — proven by a unit test injecting a fake resolveHostname.
- GIVEN assembleBuiltinTools() is called with standard deps, WHEN the returned AgentTool[] is inspected, THEN a tool named web_fetch is present (assembly test) so the model can actually see and call it.
- GIVEN pnpm test + pnpm run typecheck, WHEN scripts/verify-gates.sh runs, THEN it exits 0 with the new RED->GREEN handler and assembly tests passing.

### P22-T3-05 — No cross-encoder reranking stage in RAG

verdict=**confirmed-undeveloped** · team=T3 · effort=M · kind=undeveloped · scope=candidate

**무엇/왜**: Plan ships hybrid pgvector+BM25+RRF but no cross-encoder reranking model to reorder retrieved chunks by relevance (Open WebUI advanced). Done (if adopted) = optional reranker port applied after hybrid retrieval.
**근거(코드베이스 검증)**: search-service.ts:54-96 hybridSearch() is the terminal ranking stage (vector cosine + bm25 keyword-count + RRF fusion + relevanceThreshold filter + topK slice) with no cross-encoder re-scoring after RRF. knowledge-search-handler.ts:97-134 flow is loadCandidates -> embed -> hybridSearch -> buildCitations with no rerank stage between. Grep for rerank/cross-encoder/RerankProvider across apps/server/src (non-test), packages/interfaces/src, db, lib = zero hits; only docs mention it. docs/OWUI-vs-WCHAT-AUDIT.md marks Reranking as not-developed (RRF only); docs/P21-BACKLOG.md:68 explicitly defers a real CrossEncoder reranker. rebuild_plan/14-INTERFACES.md and 16-API-CONTRACT.md contain no reranker type/endpoint.
**구현 지침**: Add an OPTIONAL reranker stage after hybridSearch in the T3-owned knowledge pipeline. (1) NEW apps/server/src/knowledge/rerank-provider.ts defining a LOCAL RerankProvider port (rerank(query, chunks) -> ordered/scored), mirroring the local-port pattern of embedding-provider.ts so it does NOT touch packages/interfaces. (2) NEW apps/server/src/knowledge/rerank-provider-dev-stub.ts, a deterministic dev-stub reranker (like embedding-provider-dev-stub.ts) since LOCAL_ONLY has no real cross-encoder model; real model swapped at deploy. (3) Wire into apps/server/src/tools/handlers/knowledge-search-handler.ts invoke() between hybridSearch() (~line 113) and buildCitations() (~line 127): reranker reorders/prunes the HybridSearchResult[] candidates, then final topK slice. Make it OPTIONAL (deps.reranker?) so existing RRF behavior is byte-identical when reranker is absent (this is the RED-vs-GREEN boundary). (4) Tests in apps/server/src/knowledge/**tests** and knowledge-search-handler.test.ts. FLAG shared-isolation: keep the RerankProvider port LOCAL to knowledge/ to stay T3. If the design instead puts the port type in packages/interfaces (like EmbeddingProvider) or adds a ragRerankerEnabled field to a shared org-settings interface, that portion becomes team=shared and MUST be isolated/human-gated.
**대상 파일(힌트)**: apps/server/src/knowledge/
**Acceptance**:

- GIVEN a set of retrieved candidates and an injected RerankProvider, WHEN knowledge_search runs, THEN the returned citations are ordered by the reranker score (not the raw RRF order) and re-ranks reflect the reranker output.
- GIVEN no RerankProvider is injected (deps.reranker undefined), WHEN knowledge_search runs, THEN output is identical to current RRF-only behavior (backward compatible, no regression).
- GIVEN the reranker prunes below its own relevance cut, WHEN results are assembled, THEN only the reranked topK survive and citation [N] markers remain consistent with citation-helper.
- GIVEN pnpm test and bash scripts/verify-gates.sh, WHEN run, THEN the new reranker unit tests and handler integration test pass (exit 0) with a documented RED failure before implementation.

### P22-T6-01 — No clone/duplicate chat action

verdict=**confirmed-undeveloped** · team=T6 · effort=M · kind=undeveloped · scope=candidate · **★needsBrowser**

**무엇/왜**: Open WebUI can duplicate a conversation into a new chat; no clone action is present. Done (if adopted) = clone session (copy message tree) endpoint + menu item.
**근거(코드베이스 검증)**: apps/server/src/routes/sessions.ts route inventory (GET /, /search, tags, /:id/pin, /:id/archive, /:id/messages, feedback, PATCH/DELETE /:id, active-run, HITL, artifacts) has NO POST /:id/clone|/duplicate. rebuild_plan/16-API-CONTRACT.md session endpoints (lines 418-761) define no clone endpoint. apps/web/src/components/sessions/SessionCard.tsx context menu (lines 463-508) has only 이동/고정/보관/삭제 — no clone item; props have no onClone. apps/web/src/hooks/useSessions.ts exports createSession/createFolder only. grep -rin "clone|duplicate|복제|복사본" over apps/web/src+apps/server/src returns only false positives (duplicate-key errors, migration duplicate_object, git clone in docs). packages/interfaces/src/types.ts SessionRepo (L395) has no clone method; clone can reuse existing Session/Message types so no shared-type edit is forced. message-data-access.ts already has list()+bulkInsert() primitives; session-data-access.ts has byId/deleteForOwner but no copy method.
**구현 지침**: Full-stack (spans backend + frontend; suspected file is T6 but backend route/db is outside strict T6 apps/web/src ownership — coordinate). NO packages/interfaces or packages/shared edit required (reuse existing Session + Message types; route-local SessionsPort + concrete data-access), so NOT shared. Backend: add POST /sessions/:id/clone in apps/server/src/routes/sessions.ts — verify owner via byId (auth.sub) + org scope, create a new session copying title/projectId (reuse the existing session-create path used by POST /sessions), then read source messages via message-data-access list() and re-insert via bulkInsert() into the new session, REMAPPING parentMessageId (build old-id→new-id map so the message tree relationships are preserved; drop active-run/pin state). Return the new session DTO. MUST mount is already handled (sessions router mounted) but ADD the new sub-path expectation and cross-org/404 integration test; keep envelope shape. Add a session-data-access helper (e.g. insertForOwner/copy) if no create primitive is exposed there. Frontend: add "복제" role=menuitem button in SessionCard.tsx menu (near 보관/삭제) with double-submit guard like handleArchive/handleDelete; add onClone prop; add cloneSession(id) in useSessions.ts (POST /sessions/:id/clone, prepend returned session to list); wire onClone in the sessions list/page that renders SessionCard. FLAG: new API route → update rebuild_plan/16-API-CONTRACT.md and add prefix/path to apps/server/src/**tests**/routes-mounted.test.ts EXPECTED_ROUTES per CLAUDE.md route guard.
**대상 파일(힌트)**: apps/web/src/components/sessions/SessionCard.tsx
**Acceptance**:

- GIVEN an existing session with a multi-message tree (branching via parentMessageId) WHEN POST /sessions/:id/clone is called by the owner THEN a new session id is returned and its GET /:id/messages returns the same message content with parent/child relationships preserved (remapped ids), and the original session is unchanged
- GIVEN a session owned by a different user/org WHEN POST /sessions/:id/clone is called THEN it returns 404 (no cross-org/cross-owner clone) — verified by createApp real-HTTP integration test
- GIVEN the sessions list in the browser WHEN the user opens a SessionCard hover/context menu and clicks 복제 THEN a new duplicated session appears at the top of the list (double-click does not create two clones) and opening it shows the copied conversation
- GIVEN the new route WHEN pnpm test runs THEN routes-mounted.test.ts passes with the clone path added to EXPECTED_ROUTES

### P22-T6-02 — Admin Connectors allowedTools is read-only (no editable PUT)

verdict=**confirmed-incomplete** · team=T6 · effort=M · kind=incomplete · scope=candidate · **★needsBrowser**

**무엇/왜**: ConnectorsTab renders allowedTools read-only ('이 화면에서는 읽기 전용'); only enableDirectConnections is editable, so the org tool allow-list cannot be changed from the UI. Done = editable allowedTools with a PUT save.
**근거(코드베이스 검증)**: ConnectorsTab.tsx:36-63 renders allowedTools read-only (hint: this screen is read-only); only enableDirectConnections is editable. admin-settings.ts:47-82 PUT /api/v1/admin/settings persists only org_settings JSONB via OrgSettingsSchema, which excludes allowedTools (org-settings-schema.ts:4 notes allowedModels/allowedTools are reused organizations columns, not in this schema). No admin-tools route exists: `ls apps/server/src/routes | grep tool` = NONE; no /api/v1/admin/tools in app.ts or routes-mounted.test.ts EXPECTED_ROUTES. Enabling infra is already present: packages/interfaces/src/types.ts:100 has Organization.allowedTools; auth-data-access.ts:103,112 organizations.update already writes allowed_tools via COALESCE. The mirror pattern is already shipped for allowedModels: admin-models.ts (GET/PUT /api/v1/admin/models, mounted app.ts:514) + editable ModelsGenerationTab.tsx which self-saves chips via apiFetch PUT. So allowedTools was left as the isolated read-only stub while the analogous allowedModels feature was completed in P19.
**구현 지침**: Cross-team (T1 server route + T6 UI) but NOT shared: allowedTools already exists on the Organization type (packages/interfaces/src/types.ts:100) and organizations.update already persists it (auth-data-access.ts:103,112), so NO packages/interfaces or packages/shared edit is needed and no human gate applies. Replicate the proven allowedModels pattern verbatim. T1 SERVER: create apps/server/src/routes/admin-tools.ts mirroring admin-models.ts -> GET/PUT '/' with isAdmin(role) guard, orgId derived ONLY from auth JWT (never body/query, cross-org safe), Zod { allowedTools: z.array(z.string().min(1)) }, calls deps.organizations.update(auth.org, { allowedTools }) using a hand-rolled minimal org dep (avoids touching frozen types), audit action 'admin.tools.updated'. Mount in app.ts next to the existing '/models' mount (~app.ts:512): adminApp.route('/tools', createAdminToolsRoutes({ organizations: authDa.organizations, audit: auditRecorder })). Add {GET,PUT} '/api/v1/admin/tools' to EXPECTED_ROUTES in apps/server/src/**tests**/routes-mounted.test.ts (guards the route-file-only-not-mounted gap) plus server tests for GET/PUT/403/cross-org. T6 UI: rewrite apps/web/src/components/admin/settings/ConnectorsTab.tsx to make allowedTools editable via chip add/remove + self-save (apiFetch PUT /api/v1/admin/tools body { allowedTools }), copying ModelsGenerationTab.tsx's local-state + isDirty + save/rollback flow; keep enableDirectConnections in the parent draft/save flow; remove the read-only hint. Update ConnectorsTab.test.tsx (currently asserts read-only list + hint) to assert editable input, remove buttons, and the PUT call.
**대상 파일(힌트)**: apps/web/src/components/admin/settings/ConnectorsTab.tsx
**Acceptance**:

- GIVEN an admin on the Connectors/MCP tab WHEN they add or remove a tool chip and save THEN PUT /api/v1/admin/tools is called with the updated allowedTools array and the org allow-list reflects the change (verify in browser).
- GIVEN a non-admin session or a token for another org WHEN calling PUT /api/v1/admin/tools THEN it returns 403 FORBIDDEN and no other org's allowedTools are mutated (orgId comes from JWT only).
- GIVEN the server test suite WHEN routes-mounted.test runs THEN GET and PUT /api/v1/admin/tools are present in EXPECTED_ROUTES and actually mounted.
- GIVEN ConnectorsTab rendered WHEN inspected THEN the read-only hint is gone and an editable add-input plus per-chip remove controls exist, wired to the PUT save.

### P22-T6-03 — No UI to delete/remove an indexed knowledge document

verdict=**confirmed-incomplete** · team=T6 · effort=S · kind=incomplete · scope=candidate

**무엇/왜**: DocumentsPanel/useDocuments expose upload + retry but no remove/delete, so indexed documents can't be deleted from the UI (the server DELETE /documents/:docId exists). Done = delete action wired to the existing delete route with confirm.
**근거(코드베이스 검증)**: Server DELETE fully implemented: apps/server/src/routes/documents.ts:183 app.delete("/:id", ...) -> service.deleteDocument, returns 204; mounted at /api/v1/documents (apps/server/src/app.ts:397), so endpoint is DELETE /api/v1/documents/:id. Contract at rebuild_plan/16-API-CONTRACT.md:710 (DELETE /projects/:id/documents/:docId, cascade chunk + S3 delete, 204). Frontend gap: apps/web/src/hooks/useDocuments.ts (UseDocumentsResult lines 26-35, return lines 117-126) exposes only upload + retryDocument, no delete/remove. apps/web/src/components/projects/DocumentsPanel.tsx renders upload button (line 72) + retry button for failed docs (line 159) but NO delete control. No delete/remove coverage in useDocuments.test.ts / DocumentsPanel.test.tsx (grep empty). DTO ProjectDocumentDto already defined in the hook, so no interface change needed.
**구현 지침**: Pure T6 (apps/web only) - no shared/interface or API-contract change (DTO + route + contract already exist). (1) apps/web/src/hooks/useDocuments.ts: add deleteDocument(id): Promise<void> plus deletingId state, mirroring existing retryDocument pattern (lines 93-115) - apiFetch(`/api/v1/documents/${id}`, { method: 'DELETE', credentials: 'include' }); on !res.ok setError(body.error?.message ?? '삭제에 실패했습니다.'); on success call load() to refresh list. Add both to UseDocumentsResult interface (lines 26-35) and the returned object (lines 117-126). (2) apps/web/src/components/projects/DocumentsPanel.tsx: destructure deleteDocument/deletingId (lines 50-58); add a per-row delete button (lucide Trash2 with aria-label) in the status cell action group (lines 141-169), guarded by window.confirm before calling deleteDocument(doc.id), disabled while deletingId===doc.id. Reuse existing border-border/focus-visible token utilities (no hardcoded hex). Add RED tests first: useDocuments.test.ts asserts fetch called with method DELETE + list re-fetch; DocumentsPanel.test.tsx asserts delete button present per row, confirm gate, and deleteDocument invoked.
**대상 파일(힌트)**: apps/web/src/components/projects/DocumentsPanel.tsx
**Acceptance**:

- GIVEN an indexed document row in DocumentsPanel, WHEN the user clicks the delete/remove control and confirms the window.confirm prompt, THEN useDocuments.deleteDocument issues DELETE /api/v1/documents/:id with credentials include and the document list is re-fetched so the row disappears.
- GIVEN the confirm dialog, WHEN the user cancels, THEN no DELETE request is sent and the row remains.
- GIVEN the server returns a non-2xx error on delete, WHEN deleteDocument runs, THEN error state is set and surfaced in the panel's role=alert message and the row is not removed.
- GIVEN a delete is in flight, WHEN deletingId===doc.id, THEN that row's delete button is disabled to prevent double submit.

### P22-T6-04 — Images shown only as filename chips, no thumbnail/preview

verdict=**UNVERIFIED** · team=T6 · effort=S · kind=incomplete · scope=candidate · **★needsBrowser** · **△UNVERIFIED**

**무엇/왜**: Images can be uploaded (vision input) but are rendered only as filename chips — no <img>/thumbnail in composer (ChatInput) or message bubbles (ChatView). Table-stakes for a multimodal chat. Done = image thumbnail preview in composer chips and in sent message bubbles.
**구현 지침**: Images can be uploaded (vision input) but are rendered only as filename chips — no <img>/thumbnail in composer (ChatInput) or message bubbles (ChatView). Table-stakes for a multimodal chat. Done = image thumbnail preview in composer chips and in sent message bubbles.
**대상 파일(힌트)**: apps/web/src/components/chat/ChatInput.tsx, apps/web/src/components/chat/ChatView.tsx, apps/web/src/hooks/useAttachments.ts

### P22-T6-05 — Queue messages while a response is generating

verdict=**confirmed-undeveloped** · team=T6 · effort=M · kind=undeveloped · scope=out-of-scope · **★needsBrowser**

**무엇/왜**: Open WebUI queues additional user messages during generation and sends them in sequence. Not in plan; sessions are Redis-locked single-run. Listed as out-of-scope.
**근거(코드베이스 검증)**: ChatInput.tsx:395-396 sets canSend=false while isStreaming and submit() early-returns at :399; ChatInput.tsx:664-682 replaces the Send button with a Stop button entirely during streaming, so no second message can be sent from the UI. useSessionStream.ts:504-510 (streamTurn overlap guard) and :1042-1048 (continueMessage) ABORT the in-flight leg when a new turn overlaps -- the inverse of queueing; there is no FIFO buffer, pending-message state, or auto-dispatch-on-stream-end (send at :979; streamTurn finally resets isStreaming at ~~:968/:1113). All rebuild_plan 'queue' references (14-INTERFACES.md, 16-API-CONTRACT.md:82, 03-ARCHITECTURE.md) are the server-side Redis job/HITL queue and ToolCallStatus='queued', not client-side message queueing. 19-UIUX-UPGRADE.md 19.8 out-of-scope list does not include it. No component, hook, or test implements queuing additional user messages during generation.
**구현 지침**: Frontend-only, T6, no shared/interface change (reuses existing send()/SendOptions and POST /messages; the Redis session lock releases at run end so sequential post-stream dispatch is compatible). (1) apps/web/src/hooks/useSessionStream.ts: add a queue buffer (useRef array of {content, attachments, options}) plus queued-messages state exposed from the hook return (~~:1322). When send() (:979) is called while isStreamingRef.current is true, push to the queue INSTEAD of hitting streamTurn's abort-on-overlap guard (:504-510) -- keep abort-on-overlap ONLY for edit/regenerate/continue, not for user queued sends. In the streamTurn finally block that resets isStreamingRef.current=false (~:968 and :1113), if the queue is non-empty, shift the head and dispatch it via streamTurn (FIFO). (2) apps/web/src/components/chat/ChatInput.tsx: while isStreaming, let submit() enqueue instead of early-returning at :399, and render a queued-message chip row (reuse attachment-chip pattern) with per-item remove/cancel; keep the Stop button visible for the active run. No new interface or API contract required.
**대상 파일(힌트)**: apps/web/src/components/chat/ChatInput.tsx
**Acceptance**:

- GIVEN a response is currently streaming, WHEN the user types a message and presses Enter, THEN the message is added to a visible queue (not discarded) and the in-flight stream continues uninterrupted (not aborted).
- GIVEN one or more queued messages, WHEN the current response finishes streaming, THEN the next queued message is automatically dispatched as a new turn in FIFO order until the queue drains.
- GIVEN a queued (not-yet-sent) message, WHEN the user removes/cancels it, THEN it is dropped and never sent.
- BROWSER: GIVEN the chat UI, WHEN two messages are entered during generation, THEN both appear as queued and are delivered sequentially after the active response completes, each producing its own assistant turn.

### P22-T6-06 — No multi-model side-by-side comparison chat

verdict=**confirmed-undeveloped** · team=T6 · effort=L · kind=undeveloped · scope=candidate · **★needsBrowser**

**무엇/왜**: Plan's multi-LLM work is per-turn model selection/routing, not simultaneous side-by-side answers. Open WebUI lets you send one prompt to 2+ models and compare in parallel columns. Not scheduled by the plan. Done (if adopted) = parallel model fan-out + column UI + sibling navigation.
**근거(코드베이스 검증)**: Single-model only across the whole stack. UI: apps/web/src/components/chat/ModelModePicker.tsx has one select (data-testid=model-picker-model) bound to model:string / onModelChange, no compare/columns mode. Send: apps/web/src/hooks/useSessionStream.ts L54/119 model?:string (singular), L941 spreads a single {model} into one run. Server: apps/server/src/routes/messages.ts:229 model:string; apps/server/src/orchestrator/model-router.ts selectModel() returns exactly one model per turn. No compare route in apps/server/src/**tests**/routes-mounted.test.ts EXPECTED_ROUTES; repo-wide grep for compare|side-by-side|multiModel|fanOut hits only localeCompare/code-interpreter/unrelated. No comparison type in rebuild_plan/14-INTERFACES.md or 16-API-CONTRACT.md; no side-by-side/parallel-column mention anywhere in rebuild_plan/. The siblings/childrenOf logic in useSessionStream.ts and routes/sessions.ts is message-tree branching (one active branch shown at a time), NOT parallel multi-model columns.
**구현 지침**: Goal: one prompt fanned out to 2+ selected models, streamed into parallel columns with per-column sibling navigation (Open WebUI-style). T6 files: ModelModePicker.tsx (add multi-select/compare mode so more than one model can be chosen), ChatInput.tsx (pass models[] through send options), useSessionStream.ts (fan out N parallel runs, one per model, keying each stream/assistant message by message_start.meta.model which already carries the model; group them as per-column siblings), ChatView.tsx (columnar layout, one column per model, each with its own prev/next sibling controls reusing existing childrenOf machinery). MINIMAL PATH stays in T6: reuse the existing single-model run endpoint by firing N parallel POST runs (one model each) and distinguishing streams via message_start.meta.model. FLAG: a clean backend fan-out that accepts models[] in one run request would require a NEW API contract in rebuild_plan/16-API-CONTRACT.md and likely a shared request-type change => team shared, must isolate/human-gate. Prefer the client-side fan-out to keep it within T6 without touching packages/interfaces or packages/shared.
**대상 파일(힌트)**: apps/web/src/components/chat/ChatView.tsx
**Acceptance**:

- GIVEN a user in a chat session WHEN they enable compare mode and select 2+ allowed models and send one prompt THEN each selected model streams its answer into its own parallel column simultaneously
- GIVEN two model columns rendered side by side WHEN each column has multiple regenerated answers THEN per-column prev/next sibling controls navigate that column's answers independently without affecting the other column
- GIVEN a browser session (needs_browser) WHEN compare mode is toggled and a prompt is sent THEN the column UI, live streaming per model, and message_start.meta.model labeling are visually verified in the running app
- GIVEN a single selected model WHEN a prompt is sent THEN behavior is unchanged (single-column) confirming backward compatibility

### P22-T6-07 — No PWA / installable app manifest

verdict=**confirmed-undeveloped** · team=T6 · effort=M · kind=undeveloped · scope=candidate · **★needsBrowser**

**무엇/왜**: No installable Progressive Web App (standalone window, home-screen icon). Open WebUI common. Done (if adopted) = web app manifest + service worker + install affordance.
**근거(코드베이스 검증)**: apps/web/public/ holds only an empty .gitkeep (no manifest.webmanifest, no icons, no sw.js). No service worker or navigator.serviceWorker registration anywhere in apps/web/src (grep for serviceworker/workbox/next-pwa/serwist/beforeinstallprompt = 0 hits). apps/web/package.json has no PWA dependency. apps/web/src/app/layout.tsx metadata is only { title: WChat } with no manifest/themeColor/appleWebApp/icons; no apps/web/src/app/manifest.ts route exists. The only 'standalone' match is next.config.ts output:'standalone' which is a Next.js Docker build target, not a PWA display mode (false positive). rebuild_plan/ contains no PWA/service-worker/installable/manifest/home-screen references.
**구현 지침**: All changes confined to apps/web (T6; no packages/interfaces or packages/shared edits, no API contract change). Files: (1) Web app manifest via Next.js metadata route apps/web/src/app/manifest.ts (MetadataRoute.Manifest served at /manifest.webmanifest) or static apps/web/public/manifest.webmanifest — fields: name 'WChat', short_name, start_url '/', display 'standalone', background_color/theme_color = Hyundai WIA primary #00287A per apps/web/DESIGN.md tokens, icons 192px + 512px + maskable variant. (2) Icon assets in apps/web/public/ (icon-192.png, icon-512.png, apple-touch-icon.png). (3) Service worker apps/web/public/sw.js (minimal app-shell/offline cache) + client-component registrar apps/web/src/components/ServiceWorkerRegister.tsx calling navigator.serviceWorker.register, mounted in layout. (4) apps/web/src/app/layout.tsx: extend metadata with manifest + icons + appleWebApp, add a viewport export with themeColor. (5) Install affordance apps/web/src/components/InstallPwaButton.tsx capturing beforeinstallprompt and calling prompt() on click. FLAG (dependency decision): adopting a PWA library (next-pwa/@ducanh2912/serwist) would be a new unspecified dependency requiring plan approval — isolate if chosen. A hand-rolled manifest + minimal sw.js keeps this fully T6 with no new deps.
**대상 파일(힌트)**: apps/web/src/app/
**Acceptance**:

- GIVEN the app served in Chrome WHEN fetching /manifest.webmanifest THEN it returns a valid manifest with display:standalone, name/short_name, start_url, theme_color #00287A, and 192px+512px icons (Lighthouse PWA / installability audit passes)
- GIVEN a Chromium browser that fired beforeinstallprompt WHEN the user clicks the install affordance THEN the native install prompt appears and accepting it installs the app
- GIVEN the app installed WHEN launched from the home screen/app list THEN it opens in a standalone window with no browser URL chrome
- GIVEN the page loaded WHEN inspecting navigator.serviceWorker THEN a service worker is registered and the app shell loads on repeat/offline visit

### P22-T6-08 — Speech-to-Text voice input

verdict=**confirmed-undeveloped** · team=T6 · effort=M · kind=undeveloped · scope=out-of-scope · **★needsBrowser**

**무엇/왜**: No microphone/dictation anywhere (grep=0). Plan explicitly defers voice input (STT) to the E-tier follow-up phase (§19.8). Listed as deferred; build in the follow-up phase.
**근거(코드베이스 검증)**: grep=0 for SpeechRecognition|getUserMedia|MediaRecorder|whisper|transcrib|dictation|microphone|(Korean 'voice') across apps/web/src, apps/server/src, packages/interfaces, packages/shared, rebuild_plan/16-API-CONTRACT.md, rebuild_plan/14-INTERFACES.md. All apparent Mic/record/voice hits were false positives (Record<...> TS generics; usedMicros/budgetMicros/costMicros currency units in QuotaPanel.tsx). apps/web/src/components/chat/ChatInput.tsx composer action bar imports only Plus/AtSign/Slash/Hash/ArrowUp/Square icons -- no mic/dictation control; actions are [+][@][/], ModelModePicker, send/Stop. No transcription/audio route among apps/server/src/routes/ (auth, messages, sessions, uploads, documents, etc.). Plan explicitly defers it: rebuild_plan/19-UIUX-UPGRADE.md line 194 (section 19.8 out-of-scope / follow-up roadmap) lists voice input (STT) as an E-tier differentiator follow-up-phase item.
**구현 지침**: Deferred by plan (section 19.8, E-tier follow-up) -- build only when that follow-up phase is authorized. Minimal viable version is pure-frontend T6 using the browser Web Speech API (SpeechRecognition / webkitSpeechRecognition): add a Mic toggle button to the action bar in apps/web/src/components/chat/ChatInput.tsx (lucide Mic/MicOff icon), add a useSpeechRecognition hook under apps/web/src/hooks/ handling mic permission prompt, interim/final transcript, start/stop toggle, and unsupported-browser feature-detect (hide/disable button when API absent). Inject recognized text into the composer textarea at the cursor via the existing insert/setValue path (ChatInputHandle.setValue). No new shared type or API contract needed for this browser-only path, so it stays T6. FLAG: if a server-side STT (e.g. Whisper) is chosen instead for accuracy/cross-browser support, that requires a NEW audio-upload/transcription route in apps/server/src/routes/ + mounting in app.ts + EXPECTED_ROUTES entry in routes-mounted.test.ts + a new API contract entry in rebuild_plan/16-API-CONTRACT.md + new request/response types in packages/interfaces => becomes team 'shared' and must be isolated/human-gated.
**대상 파일(힌트)**: apps/web/src/components/chat/
**Acceptance**:

- GIVEN a browser that supports the Web Speech API, WHEN the user clicks the Mic button in the ChatInput composer and speaks, THEN recognized text is inserted into the composer textarea at the cursor and the button reflects a recording/active state.
- GIVEN the user is recording, WHEN they click the Mic button again (or press Escape), THEN recognition stops, the active state clears, and no further text is appended.
- GIVEN a browser without SpeechRecognition support (feature-detect false), WHEN the composer renders, THEN the Mic button is hidden or disabled and no runtime error occurs.
- BROWSER: GIVEN the running web app, WHEN a real user grants microphone permission and dictates a phrase, THEN the spoken phrase appears in the composer and can be sent as a message.

### P22-T6-09 — Text-to-Speech read-aloud on messages

verdict=**confirmed-undeveloped** · team=T6 · effort=M · kind=undeveloped · scope=out-of-scope · **★needsBrowser**

**무엇/왜**: No TTS playback anywhere (grep=0 for speechSynthesis/tts). Companion to STT in the deferred voice/media area. Build with the E-tier voice phase.
**근거(코드베이스 검증)**: Zero grep hits for speechSynthesis|SpeechSynthesisUtterance|tts|utterance|read-aloud across apps/ and packages/ (exit 0, empty). Zero hits for audio/speech primitives (new Audio|MediaRecorder|getUserMedia|<audio|SpeechRecognition|.play()|speak) in apps/web/src and apps/server/src. apps/web/src/components/chat/MessageActions.tsx (the hover action bar, natural home for a read-aloud button) contains only 복사/편집/재생성/👍👎/삭제/정보 — no read-aloud. rebuild_plan/16-API-CONTRACT.md, 14-INTERFACES.md, packages/interfaces, and 08-SPRINT-PLAN.md have zero voice/TTS/speech definitions. Only voice mention in all rebuild_plan is 19-UIUX-UPGRADE.md:194 listing E-tier deferred "음성 입력(STT)" — STT only, TTS not planned anywhere. Feature is genuinely absent.
**구현 지침**: Pure client-side, browser-native Web Speech API (window.speechSynthesis + SpeechSynthesisUtterance). NO server route, NO new npm dependency, NO new shared type, NO API contract change => team T6, NOT shared/isolated. Files to touch: (1) apps/web/src/components/chat/MessageActions.tsx — add a read-aloud toggle button on assistant messages (role==="assistant"), reusing the existing `content` prop; strip markdown to plaintext before speaking (reuse a small stripper or the existing Markdown pipeline). (2) New hook apps/web/src/hooks/useSpeechSynthesis.ts — manage idle/playing/paused state, feature-detect (graceful no-op / hidden button when window.speechSynthesis is undefined, e.g. jsdom), cancel utterance on unmount and on new speak, expose speak/pause/stop. (3) Tests under apps/web/src/components/chat/**tests**/ mocking window.speechSynthesis (speak/cancel/pause spies) — include RED-first per repo TDD gate; a11y parity (aria-label, aria-pressed) consistent with sibling buttons. FLAG: MVP needs NO interface/shared/contract change. ONLY if a future server-side neural TTS (audio streaming endpoint) is desired would packages/interfaces + 16-API-CONTRACT.md change be required => that variant must be isolated as team "shared"; the browser-Speech-API MVP does not.
**대상 파일(힌트)**: apps/web/src/components/chat/
**Acceptance**:

- GIVEN an assistant message rendered in ChatView, WHEN the user clicks the read-aloud (낭독) button, THEN window.speechSynthesis.speak is called with a SpeechSynthesisUtterance of the message plaintext and the button reflects a playing/aria-pressed state (jsdom test with mocked speechSynthesis).
- GIVEN TTS is playing, WHEN the user clicks the button again (toggle/stop), THEN speechSynthesis.cancel (or pause) is invoked and the button returns to idle; utterance is also cancelled on component unmount.
- GIVEN a runtime without window.speechSynthesis (feature-unsupported), WHEN MessageActions renders, THEN the read-aloud button is hidden or disabled and no error is thrown.
- BROWSER: GIVEN the app running in Chrome with an assistant reply, WHEN the user clicks read-aloud, THEN audible speech plays and pause/stop controls work and stop the audio.

## Tier 0 — 계약 배치 (휴먼게이트, Tier 3 의존성 해제)

| id         | team | eff | browser | contract | title                                                      |
| ---------- | ---- | --- | ------- | -------- | ---------------------------------------------------------- |
| `P22-C-01` | C    | M   | ·       | ·        | 계약 배치 초안(interface/shared 확장 RFC+diff, 휴먼게이트) |

### P22-C-01 — 계약 배치 초안(interface/shared 확장 RFC+diff, 휴먼게이트)

verdict=**N/A** · team=C · effort=M · kind=infra · scope=in-plan

**무엇/왜**: 계약 배치
**근거(코드베이스 검증)**: CLAUDE.md hard rule: packages/{shared,interfaces} 수정 태스크는 격리. Tier3 20개가 여기에 의존.
**구현 지침**: Tier3(계약의존) 태스크들이 요구하는 packages/interfaces·packages/shared·DB migration 추가를 하나의 RFC(docs/rfc/P22-contract-batch.md)+제안 diff로 모아 작성하고 .ralph/CONTRACT_PENDING 를 기록 후 정지. 사람이 검토·승인하면 .ralph/CONTRACT_APPROVED 생성(승인된 항목 화이트리스트 포함). 승인 후에만 Tier3 태스크가 해당 파일 편집 허용.
**대상 파일(힌트)**: docs/rfc/P22-contract-batch.md, .ralph/CONTRACT_PENDING
**Acceptance**:

- docs/rfc/P22-contract-batch.md 에 각 Tier3 항목별 필요한 interface/shared/migration 변경이 제안 diff와 함께 명시됨
- 제안은 nullable-first·롤백경로·기존 타입 비파괴 원칙 준수
- .ralph/CONTRACT_PENDING 기록 + <CONTRACT_REVIEW_REQUESTED:P22> 신호로 정지(자동 편집 금지)
- 사람 승인 후 .ralph/CONTRACT_APPROVED 존재 시에만 후속 Tier3 진행

## Tier 3 — 계약 의존 (P22-C-01 승인 후 빌드)

| id          | team | eff | browser | contract | title                                                        |
| ----------- | ---- | --- | ------- | -------- | ------------------------------------------------------------ |
| `P22-T1-10` | T1   | S   | ·       | ✅       | Admin health/history missing from/to time-range filter…      |
| `P22-T1-11` | T1   | L   | ✅      | ✅       | LDAP / Active Directory auth                                 |
| `P22-T1-12` | T1   | L   | ✅      | ✅       | No OpenAPI tool-server ingestion                             |
| `P22-T1-13` | T1   | L   | ·       | ✅       | POST /auth/login password login endpoint                     |
| `P22-T1-14` | T1   | L   | ·       | ✅       | User-authored Python Tools / Valves / Pipe-Filter-Action…    |
| `P22-T1-15` | T1   | L   | ·       | ✅       | error_logs/health_history/message retention deletion…        |
| `P22-T1-16` | T1   | L   | ·       | ✅       | SCIM 2.0 user/group provisioning                             |
| `P22-T1-17` | T1   | L   | ·       | ✅       | SSO via OAuth/OIDC (and trusted-header)                      |
| `P22-T4-03` | T4   | L   | ·       | ✅       | ArtifactStore.cleanupExpired is a no-op; 90-day artifact…    |
| `P22-T6-10` | T6   | L   | ✅      | ✅       | No agent registry/create/edit UI (custom workspace models)   |
| `P22-T6-11` | T6   | L   | ✅      | ✅       | Arena models + ELO leaderboard / evaluation dashboard        |
| `P22-T6-12` | T6   | L   | ✅      | ✅       | Channels (real-time multi-user + @model collaboration)       |
| `P22-T6-13` | T6   | L   | ✅      | ✅       | Chat import (native JSON / ChatGPT export) not implemented   |
| `P22-T6-14` | T6   | L   | ✅      | ✅       | Add 'Connections' UI + backend to manage external…           |
| `P22-T6-15` | T6   | L   | ✅      | ✅       | No multi-language i18n framework (Korean-only UI)            |
| `P22-T6-16` | T6   | L   | ✅      | ✅       | No AI input autocomplete/copilot (ghost-text prompt…         |
| `P22-T6-17` | T6   | L   | ✅      | ✅       | Notes document workspace (editor + AI enhance)               |
| `P22-T6-18` | T6   | L   | ✅      | ✅       | Skills screen is read-only (no create/upload/enable-disable) |
| `P22-T6-19` | T6   | L   | ✅      | ✅       | No per-model cost breakdown in usage/quota + tool-metrics…   |
| `P22-T6-20` | T6   | L   | ✅      | ✅       | Hands-free voice/video call mode                             |

### P22-T1-10 — Admin health/history missing from/to time-range filter (interface change required)

verdict=**confirmed-incomplete** · team=T1 · effort=S · kind=incomplete · scope=out-of-scope · **★계약의존**

**무엇/왜**: GET /admin/health/history accepts target+limit only; contract's from/to range is unsupported because HealthHistoryRepo.recent and ToolMetricRepo.aggregate lack date params (interfaces change). Minor, deferred to interface single-source-of-truth.
**근거(코드베이스 검증)**: rebuild_plan/16-API-CONTRACT.md:946 specifies GET /admin/health/history?target&from&to with from/to ISO dates. apps/server/src/routes/admin.ts:68-90 only parses target+limit and calls deps.da.healthHistory.recent(target, limit); route comment (lines 2-3) explicitly says from/to is unimplemented because the repo contract lacks it. packages/interfaces/src/types.ts:588-591 and rebuild_plan/14-INTERFACES.md:473 define HealthHistoryRepo.recent(target, limit) with NO date params. HealthCheckResult (types.ts:322-327) has no ts field, so route-level date filtering is impossible without an interface change. NOTE: gap description is partly wrong -- ToolMetricRepo.aggregate(toolName, from, to) (types.ts:580-585) ALREADY has date params and /tool-metrics already supports from/to via parseDateRange (admin.ts:293-302); only health/history is the real gap. admin.test.ts:129-166 only covers target/403/400, no from/to test.
**구현 지침**: FLAG: requires packages/interfaces change -> MUST isolate/human-gate (CLAUDE.md path-ownership: packages/interfaces edits are blocked and human-gated). Steps once interface approved: (1) packages/interfaces/src/types.ts -- add ts:string (ISO) to HealthCheckResult AND extend HealthHistoryRepo.recent to accept a date range, e.g. recent(target, opts:{from?:Date;to?:Date;limit:number}) or add range(target,from,to,limit); mirror the identical edit in rebuild_plan/14-INTERFACES.md to keep the single-source-of-truth consistent. (2) apps/server/src/db/health-history-data-access.ts -- select created_at as ts, add AND created_at BETWEEN from AND to predicates, map row.created_at into ts in toHealthCheckResult. (3) apps/server/src/routes/admin.ts /health/history handler -- reuse the existing parseDateRange helper (already present for /tool-metrics) to read from/to and pass through to recent(). (4) Tests: apps/server/src/routes/**tests**/admin.test.ts add from/to filtering case; db test for the SQL range. ToolMetric side needs NO work (already has from/to).
**대상 파일(힌트)**: apps/server/src/routes/admin.ts, apps/server/src/db/admin-data-access.ts
**Acceptance**:

- GIVEN health_check_history rows for target=db spanning several days WHEN GET /admin/health/history?target=db&from=<iso>&to=<iso> is called as admin THEN only rows whose ts falls within [from,to] are returned
- GIVEN no from/to query params WHEN GET /admin/health/history?target=db is called THEN behavior is unchanged (most-recent up to limit, backward compatible)
- GIVEN a returned health history item WHEN inspected THEN it includes a ts timestamp field per contract 16-API-CONTRACT.md:946
- GIVEN the interface HealthHistoryRepo.recent signature changes THEN the task is isolated to .ralph/blocked_tasks as a shared/human-gated change before any implementation

### P22-T1-11 — LDAP / Active Directory auth

verdict=**confirmed-undeveloped** · team=T1 · effort=L · kind=undeveloped · scope=out-of-scope · **★needsBrowser** · **★계약의존**

**무엇/왜**: No LDAP/AD bind, attribute or group mapping. Plan defers to enterprise-governance phase. Listed as deferred.
**근거(코드베이스 검증)**: No LDAP/AD anywhere. (1) No deps: grep of package.json, apps/server/package.json, pnpm-lock.yaml for ldap|ldapjs|activedirectory|passport-ldap returns nothing. (2) No code: grep for ldap|active directory|bind|distinguishedName|sAMAccountName|memberOf across apps/server/src, apps/web/src, packages/interfaces, packages/shared yields only pino logger bindings and Function.bind - no directory auth. (3) apps/server/src/routes/auth.ts (588 lines) is magic-link + JWT only (loginUrl, dev-login, token verify); its header comment states even password auth is out of scope since the User type lacks password_hash - no provider/strategy/SSO/LDAP path exists. (4) Plan defers it: rebuild_plan/19-UIUX-UPGRADE.md row 17 marks 'SSO/SCIM/RBAC/multitenant' as deferred to the governance phase, and the enterprise-governance bullet lists SSO(SAML/OIDC)+SCIM as a separate deferred phase; 12-OPS-SECURITY.md puts SSO at v1.1; 13-GLOSSARY/README name only OIDC/SAML IdP, never LDAP. (5) packages/interfaces has no auth-provider/identity-provider config type.
**구현 지침**: Genuinely absent AND explicitly deferred to the enterprise-governance phase - treat as ISOLATE/human-gated, do NOT build in a normal loop iteration. FLAGS forcing team=shared: (a) new packages/interfaces types required - LdapAuthConfig (server URL/TLS, bind DN + secret ref, baseDN, user/group search filters), attribute mapping, group-to-role mapping, and an AuthProvider/IdentityProvider abstraction the current magic-link User model lacks; (b) a new UNSPECIFIED dependency (e.g. ldapjs/ldapts) not authorized by any plan doc - must isolate per CLAUDE.md dependency rule; (c) new API contract entries in rebuild_plan/16-API-CONTRACT.md for LDAP config + directory login; (d) DB migration for provider-config + group-mapping tables (nullable-first, rollback path). Once un-gated the touch points would be apps/server/src/routes/auth.ts (new bind/login flow), a new apps/server/src/lib LDAP client, admin-settings route + apps/web admin panel for config, and packages/interfaces types.
**대상 파일(힌트)**: apps/server/src/routes/auth.ts
**Acceptance**:

- GIVEN a configured LDAP/AD server and valid directory credentials, WHEN a user submits them to the directory-login endpoint, THEN the server performs an LDAP bind, maps attributes to a User, and issues the normal access+refresh JWT session.
- GIVEN an AD user belonging to a mapped security group, WHEN they log in, THEN their org role/permissions are derived from the group-to-role mapping (and login is rejected if bind fails or the user is outside the configured baseDN).
- GIVEN an admin in the browser Settings/Identity panel, WHEN they enter LDAP connection details (host, baseDN, bind DN, group filters) and click Test/Save, THEN the config persists, a connection test succeeds, and directory login becomes available (browser-verified admin settings flow).
- GIVEN this is a shared/interfaces + new-dependency change, WHEN picked up, THEN it is isolated to blocked_tasks with rationale rather than implemented, because it requires packages/interfaces types, an unspecified dependency, and a new API contract.

### P22-T1-12 — No OpenAPI tool-server ingestion

verdict=**confirmed-undeveloped** · team=T1 · effort=L · kind=undeveloped · scope=candidate · **★needsBrowser** · **★계약의존**

**무엇/왜**: Plan integrates tools via MCP + built-ins; Open WebUI also ingests any OpenAPI (JSON/YAML) spec as callable tools. Not in plan. Done (if adopted) = OpenAPI spec import turning each endpoint into an AgentTool (overlaps MCP hardening/SSRF).
**근거(코드베이스 검증)**: Searched exhaustively: (1) apps/server/src/tools/assemble-builtin-tools.ts hardcodes 9 built-in tools only; external tools arrive solely via MCP. (2) apps/server/src/mcp/mcp-tool-adapter.ts is the ONLY spec->AgentToolSpec adapter and it targets MCP JSON-RPC (tools/list, tools/call), not OpenAPI. (3) apps/server/src/routes/ has mcp-servers.ts but no openapi/tool-server route. (4) apps/web has McpServersManager.tsx + useMcpServers.ts + /settings/mcp but no OpenAPI equivalent. (5) grep for openapi->agent/spec->tool/ingest adapters across apps/ + packages/ returned nothing (exit 1). (6) Every 'openapi' hit in the repo (apps/server/src/openapi.ts, scripts/generate-openapi.ts, apps/web/src/lib/api-types.generated.ts) is the server's OWN API-contract generation (Zod -> @hono/zod-openapi -> openapi.json -> openapi-typescript), unrelated. (7) rebuild_plan/08-SPRINT-PLAN.md, 16-API-CONTRACT.md, 14-INTERFACES.md contain zero mention of external OpenAPI/Open WebUI tool-server ingestion. Feature is genuinely absent from both code and plan.
**구현 지침**: Full-stack, contract-first feature mirroring the existing MCP-servers stack. FLAG: requires a NEW API contract — a Zod schema for an OpenApiToolServer resource (register URL/spec, list, delete, per-endpoint enable) in packages/shared/src/schemas/*.ts — which is the source-of-truth that regenerates routes/OpenAPI/web types; this makes the task team=shared / human-gated and it MUST be isolated per CLAUDE.md path-ownership rules. Note the AgentTool/AgentToolSpec interface in packages/interfaces is already SUFFICIENT (no interface change needed — mcp-tool-adapter.ts proves an external spec can map to AgentToolSpec without touching interfaces). Once the shared contract is human-approved, work splits: T1 server side — new DB migration + apps/server/src/db/schema.ts table (mirror mcp_servers), new apps/server/src/routes/openapi-tool-servers.ts (mount in app.ts createApp + add prefix to apps/server/src/**tests**/routes-mounted.test.ts), a new adapter apps/server/src/tools/openapi-tool-adapter.ts that parses an OpenAPI 3.x JSON/YAML spec and emits one AgentToolSpec per operation (operationId->name, parameters/requestBody->inputSchema JsonSchema, defaultPolicy 'hitl', permissionTier 'tool'), plus an HTTP-fetch invoke() returning AgentToolResult; REUSE apps/server/src/mcp/url-validator.ts for SSRF defense on the spec URL and each endpoint base URL (overlaps MCP hardening). Wire discovered tools into the orchestrator registry alongside assemble-builtin-tools.ts / mcp-client-pool. T6 web side — new apps/web/src/components/settings/OpenApiToolServersManager.tsx + hook + /settings route mirroring McpServersManager. Needs a YAML parser dependency (js-yaml) which is a new dependency -> must be plan-approved or isolated.
**대상 파일(힌트)**: apps/server/src/tools/
**Acceptance**:

- GIVEN a valid OpenAPI 3.x spec URL/body, WHEN an admin registers it via the new route/admin panel, THEN each operation is persisted and exposed as an AgentToolSpec (operationId->name, params->inputSchema) discoverable by the orchestrator.
- GIVEN a registered OpenAPI tool server, WHEN the model calls one of its endpoints in a chat turn, THEN the server performs an SSRF-validated HTTP request (reusing mcp/url-validator.ts) and returns an AgentToolResult, with HITL default policy enforced.
- GIVEN a spec URL pointing at a private/internal address, WHEN registration or invocation is attempted, THEN the url-validator rejects it and no request is made.
- BROWSER: GIVEN the settings admin panel, WHEN a user opens the OpenAPI tool-servers manager, adds a spec, and starts a chat, THEN the imported endpoints appear as callable tools and a tool call round-trips end-to-end in the chat UI.

### P22-T1-13 — POST /auth/login password login endpoint

verdict=**confirmed-undeveloped** · team=T1 · effort=L · kind=undeveloped · scope=in-plan · **★계약의존**

**무엇/왜**: Only magic-link and dev-login exist; the contract's POST /auth/login password path (§273, EMAIL_SENDER_KIND=noop admin/dev login + rate-limit) is entirely absent — auth.ts has no /login handler and routes-mounted.test flags it as a known gap. Done = password verify handler mounted with brute-force rate limiting, honoring the User type (blocked today because interfaces User omits password_hash, so this likely needs an interfaces change / isolation).
**근거(코드베이스 검증)**: auth.ts (588 lines, full read) has NO /login handler; only signup, magic-link, magic-link/verify, dev-login, me (GET+PATCH), logout, refresh. Lines 1-3 comment declares password path out of scope because interfaces User omits password_hash. routes-mounted.test.ts (near /auth/me entry) documents POST /auth/login (contract 273 password fallback) as a known unimplemented gap and excludes it from EXPECTED_ROUTES. packages/interfaces/src/types.ts:115-125 User = {id,orgId,email,name,role,customInstructions,status,lastLoginAt,createdAt} with no password_hash. packages/interfaces/src/DataAccess.ts:44 users repo is Repo<User,UserFilter> so a hash is never retrievable. Contract rebuild_plan/16-API-CONTRACT.md line 266 (EMAIL_SENDER_KIND=noop password fallback) and 273-292 (POST /auth/login {email,password} -> AuthMeResponse, 401 INVALID_CREDENTIALS / 403 EMAIL_DOMAIN_FORBIDDEN / 429 RATE_LIMITED) specify it. DB migration apps/server/src/db/migrations/0012_password_or_magic.sql DOES add nullable password_hash (bcrypt cost 12) but interface/repo never exposes it; grep found NO bcrypt/argon2/scrypt dep in apps/server/package.json and NO generic rate-limit middleware in lib/ or middleware/.
**구현 지침**: MUST ISOLATE (human gate): requires a packages/interfaces change. (1) Expose the password hash to the login handler: either add passwordHash to User in packages/interfaces/src/types.ts (then scrub it from every AuthMeResponse-shaped serializer in /me and /login) OR add a dedicated credential-fetch method to the users repo in packages/interfaces/src/DataAccess.ts. The DB column already exists via migration 0012_password_or_magic.sql. (2) Add POST /login handler in apps/server/src/routes/auth.ts (T1) inside createAuthRoutes: validate {email,password} or {email,magicToken}, enforce ALLOWED_DOMAINS (403 EMAIL_DOMAIN_FORBIDDEN), verify bcrypt hash (401 INVALID_CREDENTIALS), issueSession + return AuthMeResponse shape. Mount is already covered (createAuthRoutes mounted at /api/v1/auth in app.ts). (3) Add the route to routes-mounted.test.ts EXPECTED_ROUTES. (4) NEW DEPENDENCY: bcrypt/argon2 not present in apps/server/package.json and unspecified in plan => isolate. (5) NEW rate-limit mechanism for 429 RATE_LIMITED — no generic rate limiter exists in lib/ or middleware/ today; must add brute-force counter.
**대상 파일(힌트)**: apps/server/src/routes/auth.ts, apps/server/src/**tests**/routes-mounted.test.ts
**Acceptance**:

- GIVEN a user with a bcrypt password_hash WHEN POST /api/v1/auth/login with correct email+password THEN 200 with AuthMeResponse {user,org} and the _at and _rt cookies set
- GIVEN a user WHEN POST /auth/login with a wrong password THEN 401 INVALID_CREDENTIALS and no cookies set
- GIVEN repeated failed attempts beyond the brute-force threshold WHEN POST /auth/login THEN 429 RATE_LIMITED
- GIVEN an email whose domain is not in ALLOWED_DOMAINS WHEN POST /auth/login THEN 403 EMAIL_DOMAIN_FORBIDDEN

### P22-T1-14 — User-authored Python Tools / Valves / Pipe-Filter-Action functions

verdict=**confirmed-undeveloped** · team=T1 · effort=L · kind=undeveloped · scope=out-of-scope · **★계약의존**

**무엇/왜**: Open WebUI's in-app Python tool/function plugin system (Tools, Valves/UserValves, Pipe/Filter/Action functions, Pipelines) is not part of this platform — extensibility here is MCP + Skills + built-in tools. Listed as out-of-scope by design.
**근거(코드베이스 검증)**: Repo-wide grep for valves/uservalves/inlet/outlet/def-pipe/def-filter/def-action/pipelines-dir/function_module/python-function/admin-functions across apps/, packages/, rebuild_plan/ returns ZERO source matches. Only 'pipeline' hits are parserPipeline for document RAG (apps/server/src/db/document-service.ts:158) which is unrelated. No Web UI surface: grep of apps/web/src/app and apps/web/src/components for a Functions/Valves/Python admin page returns nothing. No interface/type defs in packages/. CONVERSELY the stated alternative extensibility model is fully present and wired: MCP (apps/server/src/mcp/mcp-client-pool.ts, mcp-tool-adapter.ts, url-validator.ts + routes/mcp-servers.ts 224 lines), Skills (tools/skills-engine.ts 165 lines + routes/skills.ts + skills/ dir with wchat-pptx and _template), built-in tools (tools/assemble-builtin-tools.ts assembling web_search/code_interpreter/artifact_create/knowledge_search/deep_research/memory as AgentTool[]). Design doc rebuild_plan/20-MULTI-AGENT-TOOL.md:41 defines extensibility as unified JSON Schema + MCP + built-in + Skills, with NO user-authored Python plugin runtime mentioned. This confirms the gap: Open WebUI's Python Tools/Valves/Pipe-Filter-Action/Pipelines system is genuinely absent AND out-of-scope by design.
**구현 지침**: DO NOT BUILD — out-of-scope by design (scope_class: out-of-scope). The platform's extensibility is deliberately MCP + Skills + built-in tools, not Open WebUI's in-app Python plugin runtime. Confirmed via rebuild_plan/20-MULTI-AGENT-TOOL.md (universal tool use = JSON Schema + MCP + built-in + Skills). Marked team=shared to force human-gate/isolation: if ever pursued it would REQUIRE editing packages/interfaces (new Tool/Valve/UserValve/Pipe/Filter/Action type defs — freeze violation), a new API contract in rebuild_plan/16-API-CONTRACT.md, plus a full untrusted-Python sandbox execution runtime (arbitrary user code = lethal-trifecta/RCE surface the security model in 20-MULTI-AGENT-TOOL.md §44 explicitly guards against). Recommended action: leave unimplemented / record as an explicit non-goal. No T1 build task should be spawned.
**대상 파일(힌트)**: apps/server/src/tools/
**Acceptance**:

- GIVEN the repo source tree, WHEN grepping for the Open WebUI plugin primitives (valves, inlet/outlet, def pipe/filter/action, pipelines module, function_module), THEN zero implementation or interface definitions are found — the feature is genuinely absent.
- GIVEN a user wanting to extend agent capabilities, WHEN they look for extensibility, THEN the supported paths are MCP servers (routes/mcp-servers.ts), Skills (routes/skills.ts + skills-engine.ts), and built-in tools (assemble-builtin-tools.ts) — the Python plugin system is intentionally not offered.
- GIVEN this is scope_class out-of-scope, WHEN the orchestrator triages this gap, THEN it is treated as a WON'T-BUILD/human-gated item (team=shared) and no auto-build sub-agent is spawned, because implementing it would require new packages/interfaces types + new API contract + an untrusted-Python sandbox runtime.

### P22-T1-15 — error_logs/health_history/message retention deletion unimplemented

verdict=**confirmed-incomplete** · team=T1 · effort=L · kind=incomplete · scope=out-of-scope · **★계약의존**

**무엇/왜**: data-retention 부록 H items 3/4/5 (error_logs, org.retention_days messages, health_history purge) are unimplemented: ErrorLogRepo/HealthHistoryRepo have no delete methods and message retention needs an org.retention_days column (v1.1). Requires interfaces/migration change → isolated.
**근거(코드베이스 검증)**: apps/server/src/lib/data-retention.ts implements only appendix-H items 1/2 (expired-uploads, artifactStore.cleanupExpired, artifactShares.revoke); lines 6-9 explicitly document items 3/4/5 as out-of-scope pending packages/interfaces changes. Plan rebuild_plan/12-OPS-SECURITY.md §부록H lines 501-508 requires errorLogs.deleteOlderThan(90) [item4], healthHistory.deleteOlderThan(30) [item5], and messages purge via org.retention_days [item3, marked v1.1]. But packages/interfaces/src/types.ts:567 ErrorLogRepo={append,list} and :588 HealthHistoryRepo={append,recent} have no delete method; Organization (types.ts:94) has no retentionDays field; MessageRepo (types.ts:413) extends Repo so only has delete(id) not bulk deleteOlderThan. pg impls apps/server/src/db/error-log-data-access.ts & health-history-data-access.ts contain no delete methods. RetentionDataAccess type (data-retention.ts:30) is Pick<DataAccess,"uploads"|"artifactShares"> — errorLogs/healthHistory/messages not even referenced.
**구현 지침**: SHARED / must isolate — requires packages/interfaces + migration edits (CLAUDE.md hard rule blocks in-loop implementation). Concrete work: (1) packages/interfaces/src/types.ts — add deleteOlderThan(cutoff: Date): Promise<number> to ErrorLogRepo and HealthHistoryRepo; add retentionDays: number|null to Organization; add a bulk deleteOlderThan(cutoff, orgId?) to MessageRepo (MessageRepo currently only extends Repo -> delete(id)). Mirror in rebuild_plan/14-INTERFACES.md (lines 463-476, Organization ~ same section). (2) DB migration (nullable-first): add organizations.retention_days INT NULL with rollback; no schema change needed for error_logs/health_history/messages deletes (just DELETE ... WHERE created_at < cutoff). (3) pg impls: apps/server/src/db/error-log-data-access.ts + health-history-data-access.ts add deleteOlderThan; message pg data-access add bulk delete honoring COALESCE(org.retention_days, 90). (4) Wire into apps/server/src/lib/data-retention.ts: widen RetentionDataAccess Pick to include errorLogs/healthHistory/messages/orgs, add 3 runStep() blocks (errorLogs 90d, healthHistory 30d, messages per-org retention). (5) Also note: runRetention is not yet cron-scheduled anywhere in apps/server bootstrap (grep found no caller) — schedule at 03:00 KST if wiring is in scope. Tests: extend apps/server/src/lib/**tests**/data-retention.test.ts with in-memory fakes exposing the new delete methods; RED first per TDD guide.
**대상 파일(힌트)**: apps/server/src/lib/data-retention.ts

### P22-T1-16 — SCIM 2.0 user/group provisioning

verdict=**confirmed-undeveloped** · team=T1 · effort=L · kind=undeveloped · scope=out-of-scope · **★계약의존**

**무엇/왜**: No SCIM endpoints for IdP-driven user/group create/update/deactivate. Plan defers SCIM to the enterprise-governance phase. Listed as deferred.
**근거(코드베이스 검증)**: grep -rli "scim" across apps/, packages/, rebuild_plan/ returns ONLY rebuild_plan/19-UIUX-UPGRADE.md (line 48: SSO/SCIM marked as not-adopted, deferred to the governance phase; line 195: enterprise-governance listed as a separate phase covering SSO+SCIM). apps/server/src/routes/ listing has admin.ts, admin-groups.ts, admin-grants.ts, auth.ts, etc. but NO scim route. grep "scim|provision" over packages/interfaces, packages/shared, rebuild_plan/14-INTERFACES.md, and rebuild_plan/16-API-CONTRACT.md returns zero hits — no types, no API contract, no stub. Genuinely absent and explicitly deferred out of scope.
**구현 지침**: Genuinely absent AND explicitly deferred to the enterprise-governance phase (rebuild_plan/19-UIUX-UPGRADE.md lines 48, 195). SHARED/ISOLATE: implementing SCIM requires NEW shared types (SCIM User/Group resource schemas, PATCH ops) in packages/interfaces plus a NEW section in rebuild_plan/16-API-CONTRACT.md — both are human-gated per Path ownership rules (packages/interfaces + API contract changes must be isolated). Concretely it would need: new apps/server/src/routes/scim.ts implementing SCIM 2.0 endpoints (GET/POST/PUT/PATCH/DELETE /scim/v2/Users and /Groups, plus /ServiceProviderConfig, /ResourceTypes, /Schemas); Bearer-token IdP auth distinct from user magic-link auth; mount in app.ts and add prefix to apps/server/src/**tests**/routes-mounted.test.ts EXPECTED_ROUTES; DB mapping of SCIM externalId to existing users/groups tables (admin-groups already exists) with deactivate semantics. FLAG: needs new interface/shared type + new API contract => team shared, must isolate. Recommend keeping in .ralph/blocked_tasks rather than building now: out-of-scope per plan and belongs to the separate governance phase.
**대상 파일(힌트)**: apps/server/src/routes/
**Acceptance**:

- GIVEN an IdP sends POST /scim/v2/Users with a valid SCIM 2.0 payload and a valid Bearer token, WHEN the endpoint processes it, THEN a corresponding user is created/linked via externalId and a 201 with the SCIM User resource is returned
- GIVEN an existing SCIM-provisioned user, WHEN the IdP sends PATCH /scim/v2/Users/{id} with active=false, THEN the user is deactivated (soft-disabled, cannot auth) and 200 is returned
- GIVEN a new scim route file is added, WHEN routes-mounted.test.ts runs, THEN /scim/v2 prefix is present in EXPECTED_ROUTES and the route is mounted in createApp (app.ts)
- GIVEN SCIM group provisioning, WHEN the IdP creates/updates a Group with members, THEN membership is reflected in the existing admin-groups tables and reads back via GET /scim/v2/Groups/{id}

### P22-T1-17 — SSO via OAuth/OIDC (and trusted-header)

verdict=**UNVERIFIED** · team=T1 · effort=L · kind=undeveloped · scope=out-of-scope · **★계약의존** · **△UNVERIFIED**

**무엇/왜**: Auth is magic-link + JWT/refresh; no OAuth/OIDC SSO, role/group claim mapping, or trusted-proxy header auth. Plan defers SSO (SAML/OIDC) to the enterprise-governance phase. Listed as deferred.
**구현 지침**: Auth is magic-link + JWT/refresh; no OAuth/OIDC SSO, role/group claim mapping, or trusted-proxy header auth. Plan defers SSO (SAML/OIDC) to the enterprise-governance phase. Listed as deferred.
**대상 파일(힌트)**: apps/server/src/routes/auth.ts

### P22-T4-03 — ArtifactStore.cleanupExpired is a no-op; 90-day artifact retention unimplemented

verdict=**confirmed-incomplete** · team=T4 · effort=L · kind=incomplete · scope=in-plan · **★계약의존**

**무엇/왜**: artifact-store.s3 and .inline both unconditionally return {deletedCount:0}; data-retention's artifact branch calls cleanupExpired but deletes nothing. Object-store port also lacks list/scan needed to enumerate. Done = real expiry enumeration+deletion (may need an object-store list method).
**근거(코드베이스 검증)**: Both impls are genuine no-op stubs: artifact-store.s3.ts:50-52 and artifact-store.inline.ts:49-51 each return {deletedCount:0} unconditionally (no enumeration, no deletion). data-retention.ts:63-65 calls cleanupExpired() as the artifact-store-cleanup step but nothing is deleted. object-store.ts:9-16 ObjectStore port exposes only put/get/exists/remove (no list/scan). Retention IS specified and unbuilt: 12-OPS-SECURITY.md:187 defines Artifact = 90 days, kept until share expires_at if a share link is active, and appendix H line 499 expects artifactStore.cleanupExpired() to perform it. ArtifactStore.cleanupExpired() (packages/interfaces/src/ArtifactStore.ts:23) takes no args; the s3 store is built with only an ObjectStore (app.ts:404) and the inline store with only Pick<ArtifactRepo,byId> (app.ts:403), so neither can enumerate expired rows or check active shares. ArtifactRepo (packages/interfaces/src/types.ts:467) is a bare Repo<ArtifactRecord> with filter {sessionId?,createdBy?} and no time query, unlike UploadRepo.expiredOlderThan (types.ts:507); ArtifactRecord has no expiresAt (types.ts:215-229) and the artifacts table (migrations/0006_artifacts_revisions.sql) has no expires_at column. artifact-data-access.ts has no createdBefore/olderThan query, and RLS scopes list() to the current user, so a system-wide retention job needs a BYPASSRLS/system-scoped DataAccess.
**구현 지침**: FLAG: SHARED-CONTRACT / MUST ISOLATE (human gate). A correct, spec-aligned fix requires editing packages/interfaces, which the CLAUDE.md path-ownership hard rule forbids doing inline. Concretely: (1) packages/interfaces/src/ArtifactStore.ts:23 - change cleanupExpired() to receive the deps it needs (a system-scoped ArtifactRepo + ArtifactShareRepo + cutoff), and/or (2) packages/interfaces/src/types.ts:467 - add ArtifactRepo.expiredOlderThan(cutoff) mirroring UploadRepo.expiredOlderThan (types.ts:507). Then T1 work: apps/server/src/db/artifact-data-access.ts add a system-scoped query for artifacts with created_at < now-90d AND no active (non-revoked, non-expired) artifact_share; implement real deletion in apps/server/src/lib/artifact-store.s3.ts (delete DB row + objectStore.remove(s3Key)) and artifact-store.inline.ts (delete DB row incl inline_content); wire in apps/server/src/app.ts:403-410 passing a system/BYPASSRLS DataAccess + artifactShareDa so the retention job runs org-wide, not per-caller (current artifactDa is RLS-scoped). Optionally add ObjectStore.list() in apps/server/src/lib/object-store.ts only if you want orphan-key sweeping; it is NOT sufficient/necessary for expiry since object bytes carry no created-time and inline artifacts live in the DB. data-retention.ts already calls the step, so no change there beyond passing the fuller store deps. RED test target: apps/server/src/lib/**tests**/data-retention.test.ts currently uses a fakeArtifactStore returning a fixed count; add real-store tests asserting deletedCount>0 for >90d fixtures and 0 for artifacts with an active share. Note: runRetention itself is not scheduled anywhere (no cron/bootstrap wiring found) - a separate secondary gap, out of scope here.
**대상 파일(힌트)**: apps/server/src/lib/artifact-store.s3.ts, apps/server/src/lib/artifact-store.inline.ts, apps/server/src/lib/data-retention.ts
**Acceptance**:

- GIVEN artifacts whose created_at is older than 90 days and that have no active (non-revoked, non-expired) share, WHEN runRetention/cleanupExpired runs, THEN each such artifact's DB row is deleted and its object bytes (s3Key) removed, and deletedCount equals the number removed.
- GIVEN an artifact older than 90 days that HAS an active (non-expired, non-revoked) artifact_share, WHEN cleanupExpired runs, THEN it is NOT deleted (kept until the share's expires_at) per 12-OPS-SECURITY.md:187.
- GIVEN the current no-op implementations, WHEN a new test asserts deletedCount>0 for expired fixtures, THEN it fails (RED evidence the stub does nothing) and passes only after the real enumeration+deletion is implemented.
- GIVEN the retention job runs with no per-user auth context, WHEN it enumerates expired artifacts, THEN it sees artifacts across all users via a system/BYPASSRLS-scoped DataAccess, not just the current caller's RLS-scoped rows.

### P22-T6-10 — No agent registry/create/edit UI (custom workspace models)

verdict=**confirmed-undeveloped** · team=T6 · effort=L · kind=incomplete · scope=candidate · **★needsBrowser** · **★계약의존**

**무엇/왜**: The 'agents' nav routes to the read-only skills list; there is no agent gallery/create/edit and the home agent count is a hardcoded const (BUILTIN_AGENT_COUNT=4) because no Agent interface exists in 14-INTERFACES. Open WebUI's custom workspace models (preset = base model + system prompt + tools + knowledge + access control) are a table-stakes differentiator. Done = Agent record type + registry API + create/edit UI (likely needs an interfaces addition → isolate).
**근거(코드베이스 검증)**: No Agent record type exists anywhere. In packages/interfaces the only Agent* symbols are AgentToolSpec/AgentToolInvocation/AgentToolResult/AgentTool (the tool-calling contract, NOT a workspace-model preset). rebuild_plan/14-INTERFACES.md has no Agent record type; 16-API-CONTRACT.md has no agent endpoints (grep 'agent' returns only doc-boilerplate 'agent 가 읽는'). No apps/server/src/routes/agents.ts exists (routes list confirmed); routes-mounted.test.ts EXPECTED_ROUTES has 0 'agents' matches. No agent DB migration (db/migrations 0001-0010 cover identity/sessions/projects/artifacts/memories/mcp/quotas — none for agents). No useAgents hook, no AgentGallery/AgentEditor component in apps/web/src. NavRail.tsx:30 routes the 'agents' nav item to /settings/skills — the read-only skills list (SkillsManager.tsx is a 55-line pure-display component; grep for method/fetch/POST/create/edit returns nothing). page.tsx:19 has hardcoded `const BUILTIN_AGENT_COUNT = 4;` with comment '전용 에이전트 API 가 아직 없어 정적 상수로 반영'. SMOKING GUN: SkillsManager.tsx lines 3-6 comment explicitly states the agent gallery (cards + slide-over) was excluded because no Agent registry interface exists in 14-INTERFACES.md, and must be isolated as a separate task under the no-new-types rule.
**구현 지침**: FLAG: REQUIRES NEW SHARED INTERFACE + NEW API CONTRACT => team=shared, MUST ISOLATE (human gate). Per CLAUDE.md, new types are only allowed from rebuild_plan/14-INTERFACES.md and packages/interfaces/packages/shared edits must be isolated. Scope spans all layers: (1) SHARED/BLOCKED: add an Agent record type to 14-INTERFACES.md + packages/interfaces/src (fields: id, orgId, name, description, baseModel/mode, systemPrompt, toolIds/skillIds, knowledge/projectIds, visibility/accessControl, createdBy, timestamps); add REST contract to 16-API-CONTRACT.md (GET/POST /agents, GET/PATCH/DELETE /agents/:id, org-scoped, cross-org 404). (2) T1/db: new migration e.g. apps/server/src/db/migrations/0011_agents.sql, nullable-first + rollback path + org-isolation RLS. (3) T2/server: new apps/server/src/routes/agents.ts, MOUNT in app.ts createApp, ADD prefix to **tests**/routes-mounted.test.ts EXPECTED_ROUTES, cross-org integration test. (4) T6/web: new useAgents hook (apps/web/src/hooks), AgentGallery + AgentEditor (create/edit slide-over) components under apps/web/src/components/settings, wire a real /settings/agents route, repoint NavRail.tsx:30 href from /settings/skills to the new agents route, and replace page.tsx BUILTIN_AGENT_COUNT const with a real agents.length from the hook. Depends on the interfaces PR (Phase 0.5 owner) landing first; do NOT implement in a single T6 task.
**대상 파일(힌트)**: apps/web/src/components/layout/NavRail.tsx, apps/web/src/components/settings/SkillsManager.tsx, apps/web/src/app/(app)/page.tsx
**Acceptance**:

- GIVEN an authenticated org user, WHEN they POST /api/v1/agents with {name, systemPrompt, baseModel, toolIds}, THEN a 201 with the Agent record is returned and GET /api/v1/agents lists it scoped to their org only (a different org gets 404 on GET /agents/:id).
- GIVEN the new Agent interface, WHEN typecheck runs, THEN the Agent record type resolves from packages/interfaces (added via isolated Phase 0.5 contract PR, not invented ad-hoc in a feature task).
- GIVEN a browser at /settings/agents (or the 'agents' nav item), WHEN the user opens the agent gallery and clicks create, fills the editor (base model + system prompt + tools + knowledge + access control) and saves, THEN the new agent card appears in the gallery and is editable/deletable via the slide-over.
- GIVEN agents exist, WHEN the home page loads, THEN the agents count reflects the real GET /agents length (not the hardcoded BUILTIN_AGENT_COUNT=4).

### P22-T6-11 — Arena models + ELO leaderboard / evaluation dashboard

verdict=**UNVERIFIED** · team=T6 · effort=L · kind=undeveloped · scope=out-of-scope · **★needsBrowser** · **★계약의존** · **△UNVERIFIED**

**무엇/왜**: No anonymous arena A/B models, thumbs-fed ELO leaderboard, or evaluation dashboard (message feedback rating exists but not the leaderboard/arena). Not in plan. Listed as out-of-scope.
**구현 지침**: No anonymous arena A/B models, thumbs-fed ELO leaderboard, or evaluation dashboard (message feedback rating exists but not the leaderboard/arena). Not in plan. Listed as out-of-scope.
**대상 파일(힌트)**: apps/web/src/components/admin/

### P22-T6-12 — Channels (real-time multi-user + @model collaboration)

verdict=**confirmed-undeveloped** · team=T6 · effort=L · kind=undeveloped · scope=out-of-scope · **★needsBrowser** · **★계약의존**

**무엇/왜**: No Discord-style shared rooms with threads/reactions/mentions where humans and @models converse. Not in the rebuild_plan. Listed as out-of-scope for this product's v1.
**근거(코드베이스 검증)**: Exhaustive grep, all negative for Discord-style channels. Routes (apps/server/src/routes/, 30+ files incl. sessions.ts, messages.ts, conversation-share.ts): no channels/rooms/reactions/mentions route; only 'channel' hits are Slack ALERT_SLACK_CHANNEL and AWS SSM channels. packages/interfaces + packages/shared: zero hits for channel/reaction/mention/room/realtime/presence/collaborate. rebuild_plan/16-API-CONTRACT.md: zero channel/room/reaction/websocket/collaboration endpoints. apps/server/src/db/migrations/ (30+ .sql through 0030_conversation_shares.sql): no channels/channel_members/reactions/mentions/presence tables. Realtime: zero hits for socket.io|websocket|new WebSocket|ws://|realtime|presence in apps/server/src or apps/web/src — repo has NO real-time multi-user transport (chat is SSE single-user streaming only). apps/web/src/components/chat/: single-user ChatView only, no @model/@mention composer, no reaction picker, no channel/room list. rebuild_plan/08-SPRINT-PLAN.md: no channel/collaboration task. Closest existing feature = conversation-share.ts (read-only single-conversation share), not multi-user rooms.
**구현 지침**: Genuinely absent and explicitly out-of-scope for v1 (not in rebuild_plan). FLAG: requires NEW shared types in packages/interfaces (Channel, ChannelMember, ChannelMessage, Reaction, Mention, Presence) AND a new section in rebuild_plan/16-API-CONTRACT.md AND new DB migrations — therefore team=shared, MUST be human-gated/isolated per CLAUDE.md path-ownership (no packages/interfaces or packages/shared edits without isolation). Repo also has NO real-time transport (no websocket/socket.io/presence infra anywhere); building this means a whole new realtime layer (WebSocket or SSE fan-out + presence, likely Redis pub/sub), new server routes (apps/server/src/routes/channels.ts + mount in app.ts + EXPECTED_ROUTES in routes-mounted.test.ts), new migrations (channels, channel_members, channel_messages, reactions, mentions), orchestrator hook so @model turns run in a channel context, and full new web UI (channel list, room view, threads, reactions, @mention/@model composer) under apps/web/src. Recommend leaving as out-of-scope backlog, not building in the current loop.
**대상 파일(힌트)**: apps/web/src/
**Acceptance**:

- GIVEN two authenticated users in the same org WHEN both open the same channel and one posts a message THEN the other sees it appear in real time without reload (browser check, needs_browser)
- GIVEN a channel message WHEN a user adds a reaction or replies in a thread THEN the reaction/thread persists via a channels API + DB tables and is visible to all members
- GIVEN a message mentioning @model WHEN posted THEN the orchestrator generates an assistant reply attributed to the model within the channel thread
- GIVEN the shared-type/API-contract/migration requirement THEN the task is isolated to a human gate (packages/interfaces + 16-API-CONTRACT.md changes) rather than built inline

### P22-T6-13 — Chat import (native JSON / ChatGPT export) not implemented

verdict=**confirmed-undeveloped** · team=T6 · effort=L · kind=undeveloped · scope=candidate · **★needsBrowser** · **★계약의존**

**무엇/왜**: Export exists (markdown/JSON, share), but there is no import of native JSON or ChatGPT exports. Open WebUI common. Done (if adopted) = import endpoint + UI converting native/ChatGPT JSON into sessions.
**근거(코드베이스 검증)**: Export is client-only: apps/web/src/components/chat/ShareExportMenu.tsx (exportMarkdown/exportJson call downloadTextFile on in-memory messages) + apps/web/src/lib/export-conversation.ts (conversationToMarkdown/conversationToJson; header comment states no server contract change). NO import counterpart anywhere: grep for import(Session|Chat|Conversation), /import, conversations.json, ChatGPT mapping/author.role, and Korean import verbs across apps/web/src and apps/server/src returned zero feature hits (only the TS import keyword). apps/server/src/routes/sessions.ts exposes GET /, GET /search, POST/DELETE /:id/tags, GET /:id/messages, message-feedback, DELETE /:id/messages/:mid, DELETE /:id, DELETE /:id/active-run, POST /:id/messages/hitl, GET hitl/pending, GET /:id/artifacts -- no POST create and no import/bulk route. messages.ts POST routes are only /:id/messages, /continue, /followups. No import mention in feature_list.json, rebuild_plan/16-API-CONTRACT.md, rebuild_plan/14-INTERFACES.md, or the sprint plan. Reusable building blocks exist (db/message-data-access.ts bulkInsert; db/session-data-access.ts createPgSessionDataAccess) but no HTTP endpoint wires create-session-from-imported-payload.
**구현 지침**: Full-stack feature crossing server (T1/T2) + web (T6), and it REQUIRES a new API contract plus a new shared schema, so it must be isolated/human-gated as team shared. (1) SHARED/BLOCKED: add import request/response Zod schemas to packages/shared/src/schemas (contract source of truth per 16-API-CONTRACT.md) e.g. ImportConversationsRequest{format:'native'|'chatgpt', payload} and ImportConversationsResponse{createdSessionIds:string[]}; regenerate via pnpm gen:api-docs; add the endpoint to 16-API-CONTRACT.md and any new DTO to 14-INTERFACES.md. (2) Server (T1/T2): new route apps/server/src/routes/imports.ts (or POST /sessions/import); parse native format ({title,messages} from conversationToJson) AND ChatGPT export (conversations.json array with title + mapping graph -> flatten by parent pointers, filter author.role user/assistant, extract message.content.parts). For each conversation create a session (session-data-access) then message-data-access bulkInsert user/assistant rows scoped to caller org/user. Mount in app.ts createApp and add prefix to apps/server/src/**tests**/routes-mounted.test.ts EXPECTED_ROUTES. (3) Web (T6): add an Import entry + hidden file input in ShareExportMenu.tsx (or a new ImportConversationsDialog), read the file client-side, POST to the new endpoint, then refresh the session list. Add tests: server unit tests for both parsers + cross-org isolation, and a ShareExportMenu/import UI test.
**대상 파일(힌트)**: apps/web/src/components/chat/ShareExportMenu.tsx
**Acceptance**:

- GIVEN a valid native export ({title,messages}) POSTed to the import endpoint, WHEN the request is processed, THEN a new session is created for the caller org and its user/assistant messages are persisted in order and returned session id is in createdSessionIds.
- GIVEN a ChatGPT conversations.json (array of conversations with a mapping graph), WHEN imported, THEN each conversation becomes one session with messages flattened via parent pointers, only user/assistant roles kept, content.parts joined.
- GIVEN a user from org A importing, WHEN sessions are created, THEN they are scoped to org A only and not visible cross-org (isolation test).
- GIVEN the chat UI (browser), WHEN the user picks Import in the share/export menu and selects a native or ChatGPT JSON file, THEN new sessions appear in the session list and open with the imported messages rendered.

### P22-T6-14 — Add 'Connections' UI + backend to manage external OpenAI-compatible provider base-URL + API keys (enable/verify)

verdict=**confirmed-undeveloped** · team=T6 · effort=L · kind=undeveloped · scope=candidate · **★needsBrowser** · **★계약의존**

**무엇/왜**: There is no Open-WebUI-style Connections screen to add/manage external provider base-URLs + API keys; model availability is admin allowedModels chips only, and ApiKeysManager issues WChat's own outbound keys (not provider connections). Done = admin/user connection management (OpenAI-compatible base URL + key, enable/verify), if in scope for LOCAL_ONLY multi-provider.
**근거(코드베이스 검증)**: No Open-WebUI-style "Connections" feature exists anywhere in the repo (verified by search, not assumption):

- apps/web/src/components/settings/SettingsIndex.tsx:17-60 — lists 7 sections (memories/skills/mcp/prompts/api-keys/quota/profile); NONE for external provider connections.
- apps/web/src/components/admin/settings/ModelsGenerationTab.tsx:43-90,206-277 — allowedModels are plain model-ID string chips added/removed and PUT to /api/v1/admin/models; there is NO base-URL, provider-key, enable, or verify field.
- apps/web/src/components/admin/settings/ConnectorsTab.tsx:8-33 — only a single enableDirectConnections boolean toggle + READ-ONLY allowedTools chips (MCP/tools scope). Not a model-provider endpoint/key manager.
- apps/web/src/components/settings/ApiKeysManager.tsx:19-60 — issues WChat's OWN outbound keys via /api/v1/api-keys (external clients auth INTO WChat) — the reverse of provider connections, exactly as the gap describes.
- apps/server/src/routes/ — no connections/providers route; admin-models.ts:14-98 manages only allowedModels: z.array(z.string()). routes-mounted.test.ts expected routes contain no connections/providers prefix.
- apps/server/src/app.ts:188-201 — the LLM provider is built ONLY from env.ANTHROPIC_API_KEY (new Anthropic({apiKey}), no baseURL); code comment "오늘은 concrete provider 가 하나뿐" (only one concrete provider today). llm-provider-openai.ts (OpenAI-compatible) exists but is never wired from any user/admin-supplied base URL + key.
- apps/server/src/db/migrations/ — no provider/connection/endpoint table.
- packages/interfaces — grep for ProviderConnection/ModelConnection/Connection => NONE.
- rebuild_plan/16-API-CONTRACT.md, 14-INTERFACES.md, 08-SPRINT-PLAN.md — no connection/multi-provider/base-url endpoint or type or task (grep negative).
  **구현 지침**: Genuinely absent end-to-end; building it fully crosses SHARED boundaries so it MUST be human-gated/isolated per CLAUDE.md (packages/interfaces + new API contract). Concrete surface:

1. SHARED (isolate): new ProviderConnection DTO in packages/interfaces (id, orgId/userId, name, provider kind, baseUrl, encrypted apiKey/keyPrefix, enabled, verifiedAt, models[]) — editing packages/interfaces triggers the human-gate rule.
2. SHARED (isolate): new API contract in rebuild_plan/16-API-CONTRACT.md for /api/v1/connections (GET/POST/PATCH/DELETE) + POST /api/v1/connections/:id/verify.
3. T1/db: new nullable-first migration for a provider_connections table with RLS (org scope + optional user scope) and encrypted key column; mirror api_keys masking (return keyPrefix only on re-read).
4. Server route (routes/connections.ts) must be mounted in app.ts createApp AND added to **tests**/routes-mounted.test.ts EXPECTED_ROUTES (per CLAUDE.md route-guard rule); verify endpoint should do a lightweight models/completions probe with SSRF/url-validation reuse from mcp/url-validator.ts.
5. T2 orchestrator: extend app.ts:198 createLLMProviderRegistry to build providers dynamically from stored connections (reuse existing createOpenAILLMProvider in orchestrator/llm-provider-openai.ts, which already takes an injected client with configurable baseURL) instead of only env.ANTHROPIC_API_KEY.
6. T6: add a "연결/Connections" entry to SettingsIndex.tsx (+ new /settings/connections route) and/or a new admin tab alongside ModelsGenerationTab; new ConnectionsManager.tsx (card layout matching ApiKeysManager/PromptsManager) with add/edit base-URL+key, enable toggle, verify button, masked key display.
   SCOPE FLAG: gap description itself says "if in scope for LOCAL_ONLY multi-provider" — this is a candidate/scope decision (system is currently single-provider env-only), so a human must confirm scope before build.
   **대상 파일(힌트)**: apps/web/src/components/settings/SettingsIndex.tsx, apps/web/src/components/admin/settings/ModelsGenerationTab.tsx
   **Acceptance**:

- GIVEN an admin on the settings Connections screen, WHEN they enter an OpenAI-compatible base URL + API key and save, THEN a provider_connections row is persisted (key stored encrypted, only keyPrefix returned on re-read) and the connection appears enabled in the list.
- GIVEN a saved connection, WHEN the admin clicks Verify, THEN the server performs a scoped probe to the base URL and the UI shows verified/failed status without leaking the full key.
- GIVEN an enabled connection whose models are allowed, WHEN a user starts a chat selecting one of those models, THEN the orchestrator LLMProviderRegistry routes the turn through that connection's baseURL/key (not env.ANTHROPIC_API_KEY).
- GIVEN cross-org isolation, WHEN org A requests org B's connection, THEN it returns 404/forbidden and RLS prevents access (verified via createApp integration test + browser check of the admin/settings Connections panel).

### P22-T6-15 — No multi-language i18n framework (Korean-only UI)

verdict=**confirmed-undeveloped** · team=T6 · effort=L · kind=undeveloped · scope=candidate · **★needsBrowser** · **★계약의존**

**무엇/왜**: UI is Korean-only; there is no internationalization/language selection. Open WebUI ships multilingual support. Done (if adopted) = i18n scaffolding + per-user language selection.
**근거(코드베이스 검증)**: No i18n framework exists anywhere. (1) Zero i18n deps in any package.json (rg for react-intl|next-intl|@lingui|i18next|react-i18next|@formatjs|vue-i18n across all non-node_modules package.json = 0 hits). (2) No locale/messages/translation/i18n directories under apps/web. (3) No language provider/context/selector (LanguageProvider|useLanguage|changeLanguage|setLanguage = 0 hits). (4) All 'locale'/'language' grep hits are false positives: toLocaleDateString('ko-KR') in apps/web/src/lib/promptVariables.ts:13 & components/settings/QuotaPanel.tsx:16 & components/projects/DocumentsPanel.tsx:43, localeCompare string sorts in components/admin/AdminUsersManager.tsx:39 & apps/server/src/routes/usage.ts:65, and code-interpreter Language='python'|'javascript'|'bash' type in tools/handlers/code-interpreter-handler.ts. (5) User interface at packages/interfaces/src/types.ts:115-125 has NO language/locale/preferredLanguage field. (6) rebuild_plan/16-API-CONTRACT.md and 14-INTERFACES.md define no locale endpoint/field; 08-SPRINT-PLAN.md never mentions i18n/다국어/국제화. UI is hardcoded Korean-only throughout.
**구현 지침**: SHARED / MUST ISOLATE (human gate). The full done-definition "i18n scaffolding + per-user language selection" cannot be built by the loop because it requires: (a) adding a `language`/`preferredLanguage` field to the `User` interface in packages/interfaces/src/types.ts:115 (interface change → isolate per CLAUDE.md), (b) a matching REST contract addition in rebuild_plan/16-API-CONTRACT.md + server route (e.g. PATCH /api/me or /api/users/:id preferences) to persist it (new API contract → isolate), and (c) a new i18n dependency (react-i18next/i18next or next-intl) NOT specified in any plan doc (unspecified dependency → isolate). Purely client-side scaffolding that COULD be T6-only without isolation: install i18n provider wrapping apps/web/src/app layout, create apps/web/src/lib/i18n/ with ko + en message catalogs, extract hardcoded Korean strings from components/{settings,admin,chat,projects}, add a LanguageSelector into components/settings/ProfileManager.tsx or SettingsIndex.tsx storing choice in localStorage. But per-user server-side persistence (the stated acceptance) forces the shared/interface + contract + dependency changes above → route to human gate, append to .ralph/blocked_tasks with rationale (needs User.language interface field + API contract + new i18n dep). Follow-up: also localize toLocaleDateString('ko-KR')/₩ formatting (promptVariables.ts:13, QuotaPanel.tsx:16, DocumentsPanel.tsx:43) via the chosen locale.
**대상 파일(힌트)**: apps/web/src/
**Acceptance**:

- GIVEN a fresh apps/web build WHEN grepping for react-i18next|next-intl|i18next in apps/web/package.json THEN a declared i18n dependency exists and an apps/web/src/lib/i18n message-catalog module loads ko + at least one non-ko locale
- GIVEN a logged-in user in the browser WHEN they open Settings/Profile and switch language from Korean to English THEN visible UI labels (nav, settings, chat controls) re-render in English without a full page reload
- GIVEN a user selected English and the User.language field + preferences API exist WHEN they log out and log back in THEN the app loads in English (choice persisted server-side per user, not just localStorage)
- GIVEN the interface/contract/dependency changes are required WHEN the loop reaches this task THEN it is isolated to .ralph/blocked_tasks (packages/interfaces User.language + API contract + new dep) rather than implemented

### P22-T6-16 — No AI input autocomplete/copilot (ghost-text prompt completion)

verdict=**confirmed-undeveloped** · team=T6 · effort=L · kind=undeveloped · scope=candidate · **★needsBrowser** · **★계약의존**

**무엇/왜**: Open WebUI offers real-time prompt completion (Tab to accept) via the task model. Not scheduled by the plan. Done (if adopted) = autocomplete overlay backed by a fast task model with admin+user enable.
**근거(코드베이스 검증)**: Read apps/web/src/components/chat/ChatInput.tsx in full (688 lines): implements /slash, @mention, #document triggers, draft persistence, autogrow, ModelModePicker — but onKeyDown (L426-456) has NO Tab-to-accept branch and the JSX renders NO ghost-text/suggestion overlay. Server: ls apps/server/src/routes/ (27 files) shows no completion/autocomplete route; grep 'autocomplete|task.?model|/complet|ghost|suggest' over apps/server/src returned nothing; routes-mounted.test.ts has no such prefix. No entry in rebuild_plan/16-API-CONTRACT.md, 14-INTERFACES.md, packages/interfaces/, or 08-SPRINT-PLAN.md (matches gap desc 'Not scheduled by the plan'). Adjacent-but-different: apps/server/src/orchestrator/followups.ts (generateFollowups) and session-title-tags.ts do short auxiliary LLMProvider.chat calls for next-turn suggestion chips / titles, not real-time input completion.
**구현 지침**: Full-stack candidate feature, currently absent. (1) NEW backend endpoint e.g. POST /api/v1/completions taking current draft + short context, calling a fast task model via LLMProvider.chat — reuse pattern in apps/server/src/orchestrator/followups.ts (generateFollowups) / session-title-tags.ts. Add route file under apps/server/src/routes/, mount in app.ts createApp, and add prefix to apps/server/src/**tests**/routes-mounted.test.ts EXPECTED_ROUTES. (2) FLAG-SHARED: requires a NEW API CONTRACT entry in rebuild_plan/16-API-CONTRACT.md and a NEW request/response type — must be isolated/human-gated per CLAUDE.md ('새 타입 없으면 격리', 'shared/interface 변경 → 즉시 격리'). (3) Admin enable: extend org_settings (lib/org-settings-schema.ts local Zod) + admin-settings route/UI toggle. (4) User enable: user settings toggle. (5) Frontend T6 (apps/web/src): add ghost-text overlay + debounced fetch hook in ChatInput.tsx, Tab key branch in onKeyDown to accept, ESC/typing to dismiss, cancel in-flight on change. Cannot be done purely inside T6 ownership because it needs a backend endpoint + contract → isolate.
**대상 파일(힌트)**: apps/web/src/components/chat/ChatInput.tsx
**Acceptance**:

- GIVEN autocomplete enabled at org+user level WHEN a user pauses while typing a partial prompt in ChatInput THEN a fast task-model completion is fetched and rendered as inline ghost text after the caret, and pressing Tab accepts it into the textarea (browser check)
- GIVEN the admin settings panel WHEN an admin toggles the autocomplete feature off THEN POST /api/v1/completions is gated (403/feature-disabled) and no ghost text appears for any user in that org (browser + API check)
- GIVEN a completion request is in flight WHEN the user keeps typing or presses Escape THEN the pending request is cancelled/ignored and stale suggestions never overwrite newer input
- GIVEN the endpoint and its request/response type do not yet exist in 16-API-CONTRACT.md / interfaces THEN the task is isolated (blocked_tasks / human gate) before any packages/interfaces or contract edit is made

### P22-T6-17 — Notes document workspace (editor + AI enhance)

verdict=**confirmed-undeveloped** · team=T6 · effort=L · kind=undeveloped · scope=out-of-scope · **★needsBrowser** · **★계약의존**

**무엇/왜**: No standalone Notes editor with AI enhance/context-injection/agentic access. Not in the plan. Listed as out-of-scope.
**근거(코드베이스 검증)**: No Notes editor exists. find apps/web/src -iname _note_ -o -iname _editor_ returns nothing. The only notes strings are a sample filename notes.md (apps/web/src/app/preview/page.tsx:65) and a release note line in 08-SPRINT-PLAN.md. The documents.ts route (apps/server/src/routes/documents.ts) is Project Documents = multipart RAG file uploads (parse to chunk to embed, per 16-API-CONTRACT.md section 5), NOT a notes editor: no editor / AI-enhance / context-injection surface. rebuild_plan/14-INTERFACES.md defines no Note entity or DTO (only ProjectDocument/ProjectDocumentRecord/DocumentChunk). 16-API-CONTRACT.md has no /notes endpoints. Web app routes under apps/web/src/app/(app) are chat, projects, settings, admin, share — no notes route. Feature genuinely absent and out-of-scope / not-in-plan as claimed.
**구현 지침**: FLAG: REQUIRES new shared contract — must be ISOLATED / human-gated (out-of-scope, not in plan). Building it would need: (1) packages/interfaces: new Note entity + NoteRecord + Note DTO Zod schema added to 14-INTERFACES.md (only ProjectDocument exists today). (2) rebuild_plan/16-API-CONTRACT.md: new /notes CRUD endpoints + an AI-enhance endpoint (no notes section today). (3) T1: apps/server/src/routes/notes.ts (mount in app.ts createApp + add prefix to apps/server/src/**tests**/routes-mounted.test.ts EXPECTED_ROUTES) + db/note-service.ts + DB migration for a notes table (nullable-first, org/project scoped, RLS, rollback path). (4) T2: apps/server/src/orchestrator integration for AI-enhance + context-injection (inject note content into chat context) + agentic tool access to notes. (5) T6: apps/web/src/app/(app)/notes/ route + apps/web/src/components/notes/ (markdown/rich-text editor component, AI-enhance action, context-inject toggle). Because it edits packages/interfaces and the API contract, per CLAUDE.md hard rules this is a blocked_tasks isolation item, not a direct implementation.
**대상 파일(힌트)**: apps/web/src/
**Acceptance**:

- GIVEN packages/interfaces has no Note type WHEN grepping for 'interface Note' or 'Note DTO' in 14-INTERFACES.md THEN nothing is found, confirming a new shared type is required (isolate).
- GIVEN a logged-in user WHEN they navigate to a /notes route in the browser THEN they can create/edit a standalone note in a rich-text/markdown editor and it persists across reloads.
- GIVEN a note being edited WHEN the user clicks AI-enhance THEN the note content is improved via the orchestrator, and WHEN they toggle context-inject THEN the note content is injected into the chat context and reachable by agentic tools.
- GIVEN this is marked out-of-scope and needs packages/interfaces + API-contract changes THEN the task is appended to .ralph/blocked_tasks for human gate rather than implemented directly.

### P22-T6-18 — Skills screen is read-only (no create/upload/enable-disable)

verdict=**confirmed-undeveloped** · team=T6 · effort=L · kind=incomplete · scope=candidate · **★needsBrowser** · **★계약의존**

**무엇/왜**: SkillsManager is a read-only listing via useSkills — no install/upload/create/edit/toggle, unlike Open WebUI tools/functions which are user-authorable. Done = skill upload/create/enable-disable actions backed by skill-registry routes.
**근거(코드베이스 검증)**: SkillsManager.tsx (55 lines) is read-only: renders useSkills() list only, no create/upload/edit/toggle UI; its own comment L4-6 admits authoring was excluded for lack of a defined interface (new-types-forbidden rule). useSkills.ts only does GET /api/v1/skills, no mutation functions. routes/skills.ts + skill-assets.ts expose ONLY GET (/, /:id/SKILL.md, /:skillId/:filename); zero POST/PUT/PATCH/DELETE; routes-mounted.test.ts EXPECTED_ROUTES confirms only GETs are mounted. packages/interfaces/src/SkillRegistry.ts interface exposes only list/byId/reload, NO create/upsert/delete/enable/disable. 16-API-CONTRACT.md section 11 defines only the 3 GET endpoints. Migration 0009_mcp_servers_skill_assets.sql creates skill_assets (binary assets, admin-only modify RLS) but NO skill-definition table and no enable/disable state; skills-engine.ts loads skills purely from skillsDir filesystem and treats them as immutable server-only assets. feature_list.json L518 acceptance text says CRUD API but only Read (GET) was ever implemented, and the contract/interface never specified writes.
**구현 지침**: SHARED / MUST ISOLATE + HUMAN-GATE. Not a T6-only task: no backend authoring surface exists at any layer, and the current design treats skills as filesystem-immutable server-only assets. A full build requires all of: (1) NEW INTERFACE - add mutation methods to packages/interfaces/src/SkillRegistry.ts (create/upsert/delete + enable/disable state) or a new user-skill store interface [editing packages/interfaces => shared, must isolate]. (2) NEW API CONTRACT - add POST /skills (create/upload SKILL.md + assets), PATCH/DELETE /skills/:id, and an enable/disable toggle to rebuild_plan/16-API-CONTRACT.md section 11 [new contract => shared, must isolate]. (3) NEW DB MIGRATION - a skills-definition table plus per-user/org enable/disable state; today only skill_assets exists (0009) and skills load from skillsDir with no runtime write path (architectural reversal, human decision). (4) SERVER - new write handlers in routes/skills.ts + skills-engine mutation + object-store upload for assets, mount + add to routes-mounted EXPECTED_ROUTES. (5) FRONTEND (T6) - extend useSkills.ts with create/upload/toggle/delete calls and add authoring UI (upload/create form, enable-disable toggle, edit) to SkillsManager.tsx. Recommend isolating in .ralph/blocked_tasks: requires packages/interfaces + new API contract + architectural decision on filesystem-vs-DB skill storage.
**대상 파일(힌트)**: apps/web/src/components/settings/SkillsManager.tsx
**Acceptance**:

- GIVEN an authenticated user on /settings/skills WHEN they click create/upload and submit a SKILL.md + assets THEN POST /skills persists it and it appears in the list after reload (browser check)
- GIVEN a listed skill WHEN the user toggles enable/disable THEN a PATCH/toggle endpoint persists the state and the orchestrator only injects enabled skills (browser + server test)
- GIVEN a user-authored skill WHEN the owner deletes it THEN DELETE /skills/:id removes it (and its assets) and it is gone from the list, with cross-org isolation enforced
- GIVEN the SkillRegistry interface and 16-API-CONTRACT section 11 today define only read operations WHEN implementing THEN the new mutation interface + contract endpoints must be human-gated before coding (isolation check)

### P22-T6-19 — No per-model cost breakdown in usage/quota + tool-metrics source/trend

verdict=**partial-refine** · team=T6 · effort=L · kind=incomplete · scope=out-of-scope · **★needsBrowser** · **★계약의존**

**무엇/왜**: QuotaPanel omits per-model cost (usage/me aggregates by date only, no model field) and ToolMetricsTable omits source + 7-day trend sparkline (ToolMetricDto lacks fields). Requires packages/interfaces + route extension → isolated. Listed for visibility; build only after a contract change.
**근거(코드베이스 검증)**: QuotaPanel.tsx and ToolMetricsTable.tsx both exist and are fully functional, but deliberately omit the described features (each carries an explicit code comment saying the fields aren't in the contract). Per-model cost: apps/server/src/routes/usage.ts GET /me aggregates by date only, DTO = {date,tokensIn,tokensOut,costMicros} with no model (matches 16-API-CONTRACT.md:894) — yet the source data exists: usage_logs.model column (migration 0010_quotas_usage.sql:25) and UsageLogEntry.model (packages/interfaces/src/types.ts:298). Source+trend: tool_metrics table (migration 0011_observability.sql:20) has only tool_name/status/duration_ms — no source column; ToolMetricEntry (packages/interfaces/src/types.ts:313) has no source field; admin-data-access.ts:236 toolMetricsSummary emits only count/errorRate/p50/p95/p99/last24h (no source, no 7-day trend), matching contract 16-API-CONTRACT.md:986. Web DTO ToolMetricDto in apps/web/src/hooks/useToolMetrics.ts mirrors that shape. Note packages/shared/src/schemas/ does not exist (shared is a stub), so response shapes are ad-hoc in routes + web hooks.
**구현 지침**: Two independent sub-features on an already-working base. (A) Per-model cost in QuotaPanel — data already present, needs NO packages/interfaces change: add a per-model aggregation branch to apps/server/src/routes/usage.ts GET /me (group by model, e.g. return byModel: Array<{model, costMicros, tokensIn, tokensOut}> alongside the existing daily array), extend the response shape in 16-API-CONTRACT.md section 12, extend apps/web/src/hooks/useQuota.ts (add byModel to DailyUsageEntry result), and render a per-model breakdown table in apps/web/src/components/settings/QuotaPanel.tsx. (B) Source + 7-day trend in ToolMetricsTable — FLAG SHARED, MUST ISOLATE/human-gate: requires (1) a DB migration adding a nullable `source` column to tool_metrics (migration after 0011, nullable-first + rollback path), (2) a packages/interfaces change — ToolMetricEntry gains a `source` field at packages/interfaces/src/types.ts:313 so emit sites record it (this is the isolating trigger), (3) admin-data-access.ts toolMetricsSummary extended to select source + a per-day 7-point count/error bucket for the sparkline, (4) 16-API-CONTRACT.md section 14 response shape extended with source + trend[], (5) apps/web ToolMetricDto (useToolMetrics.ts) + ToolMetricsTable.tsx render a source column and an inline SVG sparkline (reuse the polyline pattern already in QuotaPanel.tsx). Sub-feature A could ship standalone without isolation; B is the shared/gated part. Because B edits packages/interfaces, classify the whole gap as shared/isolated per CLAUDE.md hard rules.
**대상 파일(힌트)**: apps/web/src/components/settings/QuotaPanel.tsx, apps/web/src/components/admin/ToolMetricsTable.tsx
**Acceptance**:

- GIVEN a user with usage across multiple models WHEN they open /settings/quota THEN QuotaPanel shows a per-model cost breakdown table (model name + cost) sourced from GET /usage/me byModel, and the sum of per-model costs equals the month total (verify in browser).
- GIVEN GET /admin/tool-metrics WHEN an admin opens /admin/tool-metrics THEN each row shows a 출처/source column and a 7-day trend sparkline populated from the extended contract fields (verify in browser).
- GIVEN the tool_metrics migration adds a nullable source column WHEN the migration is rolled back THEN the down path drops the column cleanly and existing rows/queries are unaffected.
- GIVEN ToolMetricEntry now carries source WHEN packages/interfaces is edited THEN the task is isolated/human-gated (blocked_tasks) rather than committed under a single team, per shared-contract rules.

### P22-T6-20 — Hands-free voice/video call mode

verdict=**UNVERIFIED** · team=T6 · effort=L · kind=undeveloped · scope=out-of-scope · **★needsBrowser** · **★계약의존** · **△UNVERIFIED**

**무엇/왜**: No voice/video call UI. Plan defers full voice + computer-use to the E-tier follow-up phase. Listed as deferred.
**구현 지침**: No voice/video call UI. Plan defers full voice + computer-use to the E-tier follow-up phase. Listed as deferred.
**대상 파일(힌트)**: apps/web/src/components/chat/

## 제외/격리 (P22 태스크 아님)

| id                          | 사유                                                                                                       |
| --------------------------- | ---------------------------------------------------------------------------------------------------------- |
| `aws-provisioning`          | 배포 인프라(VPC/RDS/Redis/ECR/ECS), LOCAL_ONLY 불가 → 기존 blocked P0-T1-01 (human gate).                  |
| `ga-release-gate`           | v1.0 GA 게이트+배포 → 기존 blocked P9-ALL-01/02 (human gate).                                              |
| `a11y-playwright-gate`      | 풀스택 e2e+WCAG 감사 게이트, 스테이징 필요 → 기존 blocked P9-T6-02. (개별 a11y 개선은 각 T6 태스크에 포함) |
| `org-dynamic-toolmaxtokens` | **false-positive**: 이미 구현됨(워크플로우 검증에서 already-implemented).                                  |
