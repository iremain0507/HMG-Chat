# Changelog

이 스킬의 버전 변경 이력을 [Keep a Changelog](https://keepachangelog.com/) 형식으로 기록합니다.
버전은 `SKILL.md` frontmatter 의 `version` 및 `package.json` 의 `version` 과 항상 일치해야 합니다 (L09).

## [1.0.0] - 2026-07-13

### Added

- 최초 wchat-pptx 스킬 작성 (SKILL.md 절차 + `scripts/build.mjs` PptxGenJS 빌드 스크립트 스캐폴드).
