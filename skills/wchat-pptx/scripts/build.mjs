#!/usr/bin/env node
// wchat-pptx 스킬 entryPoint — E2B sandbox 안에서 실행될 PptxGenJS 빌드 스크립트 스캐폴드.
// sandbox 실행 엔진(apps/server/src/tools/sandbox, T1)이 아직 배선되지 않아
// pptxgenjs 의존성 설치/실제 슬라이드 생성 로직은 그 배선 완료 후 연결한다 (02-PRODUCT-VISION.md 시나리오 2).
console.log(
  "wchat-pptx build script — sandbox 실행 배선 대기 중 (T1 tools/sandbox 미구현).",
);
